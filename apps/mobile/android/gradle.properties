# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true

# GRADLE'S BUILD CACHE, ON (#916).
#
# WHAT IT BUYS. Without it `~/.gradle/caches` holds dependency modules and
# artifact transforms and no task OUTPUTS, so a runner that has never built this
# tree re-executes every compile even when the sources are byte-identical to a
# build another runner already did. With it, a task whose declared inputs match
# an entry in `~/.gradle/caches/build-cache-1` is unpacked instead of run. That
# directory is inside the path the three Android device lanes already bank
# (`.github/workflows/{ci,candidate,e2e}.yml`, `android-gradle-v2-*`), so this
# one line is what turns those lanes' warm `~/.gradle/caches` from "the
# dependencies are here" into "the compiles are here".
#
# WHY IT IS SAFE FOR THE STORE BUILD TOO, and not fenced behind a CI-only flag:
# a build cache entry is keyed on the hash of the task's declared inputs plus
# its implementation, so a task can only ever be handed an output produced from
# the same inputs by the same code. It is a correctness-preserving reuse, not a
# heuristic, which is the difference between this and caching a directory.
#
# WHAT IT DOES NOT COVER, so nobody re-derives it from a slow build: AGP's
# external-native-build (CMake/ninja) tasks are not cacheable. Those stay
# incremental the only way they can — by keeping their `.cxx` staging
# directories, which the lanes cache alongside this.
#
# `org.gradle.configuration-cache` is DELIBERATELY LEFT UNSET. It caches the
# configuration phase, not compilation, so it does nothing for the native
# compile this exists to remove; Expo SDK 57's gradle plugins do not declare
# configuration-cache compatibility; and `app/build.gradle` shells out with
# `[...].execute()` at configuration time in four places to resolve the React
# Native, Hermes, codegen and Expo CLI paths. Enabling it would trade a
# measured cost for an unmeasured one and a new class of build failure.
org.gradle.caching=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

# Specifies whether the app is configured to use edge-to-edge via the app config or plugin
# WARNING: This property has been deprecated and will be removed in Expo SDK 55. Use `edgeToEdgeEnabled` or `react.edgeToEdgeEnabled` to determine whether the project is using edge-to-edge.
expo.edgeToEdgeEnabled=true
