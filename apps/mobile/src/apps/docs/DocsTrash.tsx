// Stub for the Binding Layer v12 phone build (issue #821): this screen is
// implemented by the app build in this same change set and is never shipped
// bare — the stub only holds the route while the tree is assembled.
import React from "react";
import { View } from "react-native";

export default function DocsTrash(): React.JSX.Element {
  return <View />;
}
