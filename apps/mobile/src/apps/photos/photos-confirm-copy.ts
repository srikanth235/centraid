// THE SENTENCES PHOTOS' CONFIRMS SAY (#1015, S7).
//
// Six surfaces each spelled the same clause into their own `Alert.alert`,
// and one of them dropped it. It is the ONE thing a member deciding needs to
// know here, so it is one string, said the same way everywhere.

/** Trashing is a vault act. The photograph on the phone is not touched. */
export const TRASH_KEEPS_THE_ORIGINAL =
  "The device original is never deleted by this action.";

/** The duplicate shelves add what a kept copy keeps. */
export const TRASH_KEEPS_THE_COPY_IN_ITS_ALBUMS = `The copy you keep stays in every album it is already in. ${TRASH_KEEPS_THE_ORIGINAL}`;

/** Deleting an album is a delete of the GROUPING, never of photographs. */
export const ALBUM_DELETE_BODY = "Photos stay in the library.";
