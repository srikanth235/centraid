{
  "name": "@centraid/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Centraid desktop — Electron shell + vanilla-TS renderer. Consumes @centraid/design-tokens.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "bun run build:ts && bun run build:preload && bun run build:assets",
    "build:ts": "tsc -p tsconfig.json",
    "build:preload": "bun build src/preload.ts --target=node --format=cjs --external electron --outfile dist/preload.cjs",
    "build:assets": "mkdir -p dist/renderer && cp src/renderer/index.html src/renderer/styles.css dist/renderer/",
    "dev": "bun run build && electron .",
    "start": "electron .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@centraid/agent-harness": "workspace:*",
    "@centraid/design-tokens": "workspace:*",
    "@centraid/app-templates": "workspace:*"
  },
  "devDependencies": {
    "@centraid/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "electron": "^33.0.0",
    "typescript": "catalog:"
  }
}
