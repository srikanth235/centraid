{
  "name": "@centraid/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.mjs",
    "package": "bun run build && bun run scripts/package.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@centraid/core": "workspace:*",
    "@centraid/design": "workspace:*",
    "tldts": "7.4.9"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "sharp": "^0.35.3",
    "typescript": "catalog:"
  }
}
