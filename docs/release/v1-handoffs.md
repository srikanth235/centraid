# v1 owner hand-offs ([#1020](https://github.com/srikanth235/centraid/issues/1020))

Everything the v1 platform needs that a container with no phone, no Xcode, no Android SDK and no rented host could not do. This is the **canonical** list: the umbrella receipt records each one where it was found, and this file is where they are read together.

Every row names the **exact command**, the **evidence to hand back**, and **where the result goes** — a ledger key, a decision row, or a parked ceiling that gets promoted. A hand-off with no destination is a note; one with a destination is work.

Nothing here is a missing feature. Each is a claim this repository deliberately refused to make on hardware that could not support it: a skipped test that reads green, a DACL nobody watched refuse anybody, a ceiling filled from the wrong machine. Where a lane could not prove something it **fails loudly** or **refuses** rather than passing.

---

## 1. Devices, Xcode and the Android SDK

| # | What | Command | Evidence to hand back | Where it goes |
| --- | --- | --- | --- | --- |
| 1.1 | **A self-hosted macOS runner with one iPhone and one Android attached.** `device-android-mid` is the Samsung Galaxy A55; `device-iphone-oldest` is the iPhone XR on iOS 17. | Attach both, then set `vars.CENTRAID_DEVICE_RUNNER=true` on the repository. The four cells then run under `cargo xtask gate --profile nightly`. | The nightly run's `device-lanes` lines. A runner that claims the label with no phone attached goes **red** — that is the contract, not a bug. | Open question 13, [R-1020-20](../decisions.md#v1-platform--rust-core-kmp-shell-electron-seat-gateway-anywhere-1020). Unblocks 1.2, 1.3 and every row of section 7. |
| 1.2 | **The first real iOS compile**, which also confirms or replaces the Xcode pin. | `./gradlew -Pkotlin.native.enableKlibsCrossCompilation=true :shared:linkDebugFrameworkIosSimulatorArm64`, then `protoc-gen-swift`, then `xcodegen generate`, then `cd mobile/iosApp && swift test`. | The link time in seconds; whether `.xcode-version`'s `16.4` was accepted or which version Kotlin/Native asked for; the `swift test` result over the 19 fixtures. **It has never run** — reporting it as a pass would be the single most misleading thing this umbrella could do. | `kotlinNativeLinkSeconds` in `contracts/ledgers/compile-time.json` (currently `null`, and a null cannot be ratcheted); `.xcode-version`; [D-1020-E5a](../decisions.md#wave-3-lane-rulings-1020). Four iOS platform services are deliberately unimplemented and **refuse** rather than, for instance, writing a credential to a plist. |
| 1.3 | **The Android device build and the Macrobenchmark numbers.** | `ANDROID_HOME=… ./gradlew -Pcentraid.android=true :androidApp:assembleDebug`, then `connectedBenchmarkAndroidTest` on the named device. | The APK, and the Macrobenchmark output per journey. | The six parked `_intended` ceilings in [`tests/journeys.json`](../../tests/journeys.json). |
| 1.4 | **The Maestro flows.** | Add the 17 `testTag` / `accessibilityIdentifier` values from `mobile/maestro/flows/selectors.md` **first**, then `maestro test mobile/maestro/flows` with `MAESTRO_VERSION=2.6.1`. | One line per flow, plus the run that proves each selector matches exactly one thing. Seventeen strings nothing checks are seventeen strings that rot, which is why the two are paired. | `mobile/maestro/`, and the per-app journey rows. |
| 1.5 | **The reader pool in `crates/vault`.** Until it exists, `crates/core` serialises every call through one mutex; `CONTRACT.md` clause 3 states that honestly rather than claiming concurrency. | A pool of read connections inside `crates/vault` (the `PRAGMA` statements are SQL, and `sql-confinement` does not admit `crates/core`). | The `call-budget` p95 before and after. The cost is not currently felt — 0.47 ms against a 50 ms ceiling — so this is scheduling, not correctness. | [D-1020-D2-9](../decisions.md#wave-2-lane-rulings-1020); the issue's own words, "reads run concurrently under SQLite's own rules", are not yet true. |
| 1.6 | **`cargo mutants` on a machine with disk.** | `cargo mutants -p centraid-apps-tally --in-place` (`--in-place` is required for bundled-SQLite crates), and the same for `centraid-seat` and `centraid-apps-kit`. | The survivor list. `crates/apps/tally/src/{balance,views}.rs` is where survivors would matter most. | Wave 2's exit criterion, "cargo-mutants no survivors in `seat` and `kit`". |

## 2. Signing, notarisation and the stores

| # | What | Command | Evidence to hand back | Where it goes |
| --- | --- | --- | --- | --- |
| 2.1 | **Desktop signing enrolment.** | [enrollment.md](../enrollment.md). Apple secrets flip `CSC_IDENTITY_AUTO_DISCOVERY`; Azure secrets flip Windows signing. | The GitHub Release carrying installers. While unenrolled it **withholds the installers entirely** rather than publishing unsigned ones. | `lane-release-desktop.yml`; [docs/release.md](../release.md#enrollment--signing-secrets). |
| 2.2 | **The updater's release key.** `TRUSTED_RELEASE_KEYS` is **empty**, so every packaged update refuses with `no-trust-anchor` and relaunches instead of installing. | Enrol a key. | Enrolling flips an existing test — *"enrols no release key, so every packaged update refuses"* — which **is** the review signal that signed updates went live. Widening it to make an update path work is the "never weaken policy to go green" case. | `desktop/`. |
| 2.3 | **Mobile store enrolment.** | [enrollment.md](../enrollment.md); the native lane declares seven secrets of its own and reports `scaffold-only` without them. | The lane's own report. | `lane-release-mobile.yml`. |
| 2.4 | **Extension store enrolment, and a dev RSA key.** `contracts/extension/ids.json` carries Firefox's author-chosen id and `null` with a reason for both Chrome channels. A placeholder id was drafted and **deleted**: `native-host install` refuses an empty allowlist precisely so a host manifest cannot exist without a real id. | Chrome Web Store and AMO enrolment; `openssl` command in `extension/README.md` for a stable unpacked id. | The two real ids. | `contracts/extension/ids.json`; the lint asserts the manifests agree with it. |
| 2.5 | **The Companion's icon.** The manifests name none, so browsers show a default. | A mark from `packages/design`. | The asset. | `extension/`. A fabricated one would be worse than none. |
| 2.6 | **Where a browser host manifest is stored, per browser and per platform.** `centraid native-host install --browser chrome\|firefox --extension-id <id> --out <path>` writes it and **prints** the directory to copy it to; it never copies it itself. | A product decision: which directory an installer writes to, and whether the desktop installer should offer at all. | The decision. | Same reason `gateway install` never enables a unit: a capability that appeared because something was unpacked is a capability nobody chose to grant. |

## 3. The gateway on a real host

| # | What | Command | Evidence to hand back | Where it goes |
| --- | --- | --- | --- | --- |
| 3.1 | **The real VPS run.** The container proves the device-less half; four things only a real host can show. | The full sequence is in [docs/release.md](../release.md#owner-hand-off--the-real-vps-run) — install, `--dry-run` writes nothing, `systemd-creds`, enable, `doctor`, restart. | That `DynamicUser` + `StateDirectory` actually start (**the unit has never been loaded by a real systemd**), that `systemd-creds` hands the secret over, that the service survives a reboot, and that a seat on another machine pairs across a real network rather than over loopback. Record the transcript on the issue. | Acceptance criterion 2 of [#1020](https://github.com/srikanth235/centraid/issues/1020). |
| 3.2 | **`docker build` on a host with ordinary egress**, or with the proxy CA in the container's trust store. | `docker build -f deploy/docker/Dockerfile -t centraid-gateway:v1 .` | The image size. | `deploy/README.md`. |
| 3.3 | **The first `nix flake check`**, then commit `flake.lock`. | `nix flake check`, then add the pinned nix installer to `gate-nightly.yml`'s `flake` job. | The lock file and the pin. | The `flake` job asserts today that `flake.nix` reads its channel out of `rust-toolchain.toml` and states in the run summary that **the flake has never been evaluated** — it deliberately does not fabricate a pin for an installer this repository has no precedent for ([D-1020-G6](../decisions.md#wave-3-lane-rulings-1020)). |
| 3.4 | **The first real `prebuilt-core` matrix run.** | The `lane-prebuilt-core.yml` matrix. | Twelve `null` triples filled, plus the first `dist` artifact measurement on a non-x64 platform. | `contracts/ledgers/library-size.json` (seeded empty). |
| 3.5 | **The Windows named-pipe door.** `centraid seat --socket` exits 3 on Windows today, naming this hand-off. | Write the DACL, then **observe it refuse another user**. | The refusal, observed. A DACL that was written but never seen to refuse anybody is a claim, not a protection. | [R-1020-26](../decisions.md#v1-platform--rust-core-kmp-shell-electron-seat-gateway-anywhere-1020), [D-1020-F15](../decisions.md#wave-3-lane-rulings-1020). |
| 3.6 | **macOS and Windows desktop runs.** Everything is proven on Linux under Xvfb. | `desktop/README.md` § *Running it*; unchanged except that no `xvfb-run` is needed. | The runs. | `desktop/`. |
| 3.7 | **`xvfb` on the nightly runner**, or `desktop-e2e` and `extension-e2e` fail there. | Install it. | The nightly run. | Electron has no headless mode, and a browser test that read green with no window would be the loudest kind of lie — so these steps **fail with the install command** rather than skipping. |

## 4. Models and the network fetcher

Five model sessions and one fetcher. Each is behind `handler::Model`, a one-method trait, with `FakeModel` driving the same handler code — no `ort` session is in the crate, because that would make it unbuildable without weights and its tests dependent on a download. One row each in `crates/automations/README.md` with the exact command.

| # | Model | Size | Evidence to hand back | Where it goes |
| --- | --- | --- | --- | --- |
| 4.1 | YuNet (face detection) | 21 MB combined with OCR | A device run within a stated IoU of v0's boxes | `crates/automations/README.md`'s evidence row |
| 4.2 | ArcFace (face embedding) | 249 MB + 0.2 MB | Same-person pairs sitting **under** the party threshold | Same |
| 4.3 | PP-OCRv5 | — | A run matching v0 **character for character** | Same |
| 4.4 | CLIP | 606 MB | Image/caption cosine beating v0's | Same |
| 4.5 | Whisper | 41 MB | A transcript matching v0's | Same |
| 4.6 | **The fetcher** | — | Two runs: one against the real upstreams reporting `ready` for `faces`, and one against a **deliberately corrupted** local file reporting the sha mismatch with **no `.partial` left behind** | `centraid_media::models`; `handler::NoNetwork` already proves the "reported, never thrown" property through every line of `ensure` except the socket |

## 5. The ACP adapters

| # | What | Command | Evidence to hand back | Where it goes |
| --- | --- | --- | --- | --- |
| 5.1 | **Install the two npm adapters.** `codex` and `claude-code` speak ACP through a pinned npm package; the rest speak it natively. | `centraid assist adapters --install` **prints** the command; run it yourself. Installing an adapter downloads and then executes third-party code, which is why it is printed rather than run. | The adapter directory, and `centraid assist adapters` reporting it found. | `CENTRAID_ACP_ADAPTER_DIR`, or the default discovery path. |
| 5.2 | **One real ACP `initialize`** against a real harness. | `centraid assist preflight codex`, then a real turn. | The negotiated protocol version and the first `session/update`. **No recorded v0 wire bytes are committed**, so a divergence between v0's frames and `agent-client-protocol`'s would not be caught by the suite — this run is the only thing that would catch it ([D-1020-AS9](../decisions.md#wave-4-lane-rulings-1020)). | The umbrella receipt. |
| 5.3 | **`vault_sql` over the seat socket.** The grammar, the row cap and the principal check exist in `crates/vault`; the local channel deliberately carries only *named* reads and commands. | A local-channel message carrying a statement, plus the principal check at the seat. | Until then `centraid mcp` answers `vault_sql` with a principal refusal rather than opening the vault file itself — the tempting fallback, and the one that would make the child a second gateway. | `desktop/`'s local channel. |

## 6. CI, branch protection and the inherited reds

| # | What | Decision needed | Where it goes |
| --- | --- | --- | --- |
| 6.1 | **Branch protection must be repointed.** Open since wave 1. The required check is `check` from `ci.yml`, which no longer runs on pull requests — **and a required check that never reports blocks the pull request forever**. | Make **`gate`** and **`dependency-review`** — the two jobs in `gate.yml`, neither path-filtered, so both always report — the required checks. Branch protection is configured outside this repository. | Blocks every pull request until done. |
| 6.2 | **The gitleaks allowlist decision, twice.** `packages/model-runtime/LICENSES.md` trips `generic-api-key`. `contracts/golden/format-golden.json`'s `dataKeyHex` is the key the cross-language format golden is sealed with and **has to be in the file for the golden to be checkable at all**. | Either a reasoned `.gitleaks.toml` row naming each (with the argument that a published test vector is not a secret), or move the material out of the fixture. **Not taken by any lane**: under [R-1020-35](../decisions.md#decisions--lane-v-1020) a gate's false positive is fixed in the gate, and this is not a false positive in that sense — the string genuinely is a key and it is genuinely committed. | `.gitleaks.toml`, which no lane touched. |
| 6.3 | **The `astro@7.1.5` CRITICAL** (9.8) in `bun.lock`. | A dependency bump, outside this umbrella's scope. | `osv` stops being red. |
| 6.4 | **`lane-release-extension.yml` has no `harden-runner` step and no ledger entry.** A **release** lane is where the interesting secrets live. | Add `step-security/harden-runner` with `egress-policy: block` as the first step of the job. **The remedy is the step, not a ledger row** — the ledger is the historical allowance, not a place to put new work. | `bun run lint:ci-egress`, the `ci-policy` gate step; [docs/release.md](../release.md#what-a-release-workflow-owes-the-egress-ratchet). |
| 6.5 | **The `local` profile is over its 120 s budget on the merged tree**, and has been since three lanes' suites landed in one wave. `cargo test --workspace` is almost all of it. | Not a number to raise. Either `local` runs `cargo test` for the **changed** crates only and `pr` keeps the workspace run — which is what "incremental `cargo nextest` for one crate" already anticipates — or `cargo nextest` for its parallelism. | `crates/xtask/src/gate.rs`; `contracts/ledgers/gate-budgets.json` stays down-only either way. |
| 6.6 | **The relay-only pairing case has no runner.** `a_relay_only_pair_and_stream` is written and `#[ignore]`d with its reason: it needs egress to a relay and two networks. | Run it under `cargo test -- --ignored` on a runner with real egress; the `nightly` profile has no `--ignored` leg today. | The VPS smoke (3.1) is the proof that counts in the meantime. |

## 7. The device experiment, and the two rulings that turn on it

| # | What | Command | Evidence to hand back | Where it goes |
| --- | --- | --- | --- | --- |
| 7.1 | **The iOS background-transfer experiment.** Five app states × two transports × four measurements, over a 2,000-asset / 6 GB corpus on a **named reference device** — never a simulator. The `lldb` `_simulateLaunchForTaskWithIdentifier:` command is what makes state 4 measurable at all. | `mobile/maestro/ios-transfer-experiment.md`, in full. | Four JSON files with fixed names, the device name and iOS version, and **one line saying which row of the decision table they land in**. The four sentences are already written, so the run picks one rather than composing one. | [R-1020-3](../decisions.md#v1-platform--rust-core-kmp-shell-electron-seat-gateway-anywhere-1020) stops being provisional; [R-1020-15](../decisions.md#v1-platform--rust-core-kmp-shell-electron-seat-gateway-anywhere-1020)'s blob door ships or is deleted, and `no-listening-socket` loses its exception if it is deleted; the blank row in [mobile-offline.md](../mobile-offline.md#the-per-state-promise--the-camera-roll-backup-on-ios-provisional) is filled. Threshold: **500 assets per night, charging, on Wi-Fi**. |
| 7.2 | **Open question 10's criteria (b) and (c).** (a) has tripped at **1.72×**. (b) is *SwiftUI and Compose disagreeing on a shared state fixture more than once*; (c) is *Kotlin→Swift state delivery over 1 ms at p95 on the reference iPhone at the 50k-asset library*. | The hand-off table in `mobile/README.md`. | Both numbers. | [D-1020-X3](../decisions.md#decisions--wave-3-close-lane-x3-1020). A **second** trip flips screen contracts to sealed classes + SKIE with no further debate; both clean and protobuf stays and 1.72× is the stated price of the shared fixture. |
| 7.3 | **The OQ-10 custody boundary.** The member-key gate proves a *gateway process* cannot produce plaintext. It does **not** prove a host with root cannot read `K` off an unlocked seat's disk: the at-rest wrap is PBKDF2-600k + AES-GCM, but an unlocked session holds `K` in process memory and **no OS keychain is in the path**. | A platform keystore per seat. | The next rung. | Stated in [SECURITY.md](../../SECURITY.md#the-gateway-on-rented-hardware--v1-trust-premise-1020) with its own claim-register row, so the gate's green is not read as covering it. |
| 7.4 | **The exit criterion's wording wants amending.** *"A gateway with the key door deleted cannot produce plaintext for any `lk1:` cell, falsified against a gateway that still has the door"* cannot be met as written: a gateway that still has the door but no key produces no plaintext either, so the falsification has nothing to find. | Split it in two. | The provable pair is **the key is absent** (the search, falsified by giving it a key) and **the door is unrepresentable** (two structural tests, both falsified). | [#1020](https://github.com/srikanth235/centraid/issues/1020)'s acceptance list. Conflating them is what made the single gate read stronger than it is. |

---

## The owner questions

Each is a product judgement a lane declined to make on the owner's behalf. Each carries the options and a recommendation; adopting a recommendation is one line on the issue. The test that has to change with each answer is named, so "nothing happened" is not a possible outcome.

### People

| # | Question | Options | Recommendation |
| --- | --- | --- | --- |
| P1 | Should `social.contact_channel` be a **fourth** reading on the person sheet? Today a revoked contact scope denies the whole sheet, here as in v0. | Leave it — how to reach a person is part of who the sheet is about, and a sheet without it is arguably a lie. Or make it a fourth `ReadState`, so a member who revoked the messaging app still sees their grandfather's birthday. | **Make it a fourth reading**, on the same argument that made `tally.obligation` one. But it is a judgement about what a person sheet *is*. `a_denied_contact_plane_is_a_denied_sheet` is the test that changes. |
| P2 | Why is `people.limit` **10,000**? Five times every other app's widest window, unexplained in the manifest — and the roster now walks it for real. | Keep it; or lower it to 2,000 and match the tree. | **Ask before lowering.** A personal CRM is the one app where "every person I know" is a legitimate single view, and the walk makes the number affordable. |
| P3 | The **three dead `social` act scopes**. They grant nothing and they mislead a consent screen in both directions — the scope list understates the app's reach by three rows and overstates it by three at once. | One manifest edit in v0's `packages/blueprints/apps/people/app.json`. | Fix it in v0. Not done by the lane because the manifest is copied byte for byte on purpose and a divergence would have to be a **ported** divergence. |
| P4 | `core.merge_entity` and `core.find_duplicate_parties` — the merge plane's other two commands. | One lane, after the surface that calls them exists. | Defer. `find_duplicate_parties` is the read half of a feature whose write half is `merge_party`. |
| P5 | The **PE-F8 sweep**: does any other suite in the tree sort or hash a `serde_json::Value` by its printed text? | `crates/apps/people` was the only match for the obvious shape. | Sweep at close. [traps/serde-json-preserve-order.md](../traps/serde-json-preserve-order.md) is the doc; `kit::canonical_json` is the answer. |

### Locker

| # | Question | Options | Recommendation |
| --- | --- | --- | --- |
| L1 | Should the **audit band be a catalog entity**? Without it `locker.access` cannot work in v0, and no other surface reading `access_receipt` through the paged door can either. | Add it read-only, with the band's own access evaluation; or leave the band unreachable through the door. | **Yes, read-only** — but it is a widening of the door and wants the owner's eye. |
| L2 | Should `autofill-candidates` report **TOTP presence**? | Via a projected `IS NOT NULL`. | **Yes.** The Companion's picker is worse without it, and the column itself must never leave. |
| L3 | Should the port **decorate all four shelves** with the connector alias? | Keep v0's asymmetry; decorate all four; drop it from the live shelf too. | **Decorate all four.** A handle that depends on which screen found the row is not a handle. |
| L4 | `edit_item` **replaces** rather than patches — an edit carrying only a password clears the username, url, OTP seed and notes. Invisible to the UI, which always sends the whole draft; a hazard for an assistant or an API caller reading the manifest's optional fields as a patch API. | Make it a patch; or say in the manifest that it replaces. | **Say it replaces, now**, and file the patch as a proposal. A rename to `replace_item` would be clearer than either. |
| L5 | Make the **parity fixture's instants reproducible**. `touchUpdatedAt`'s trigger reads SQLite's `now`. | A `:ctx_now`-bound trigger — the change lane V already made to v0's conditions. | Take it. It would let the parity bundle express order for every app, not just Locker's. |

### Photos

| # | Question | Options | Recommendation |
| --- | --- | --- | --- |
| PH1 | **`media.forget_person` deletes other people's confirmed faces.** The SQL is `WHERE party_id = :party_id OR confirmed_by_party_id = :party_id`, documented as deliberate — and in a single-member vault the owner is the `confirmed_by_party_id` of *every* confirmation. So "forget me" erases every confirmed face of everyone. | (a) keep both columns; (b) delete on `party_id` only and **null** `confirmed_by_party_id` where it matches; (c) split into two commands. | **(b).** The privacy argument for deleting a party's own regions is sound; deleting the regions they *judged* is data loss about third parties, and no consumer in this tree depends on it. |
| PH2 | **`media.forget_person` executes with no confirmation** for an owner. It carries `confirm: true`, which parks a **non-owner** invocation; an owner credential — the only one a single-seat vault has — walks straight through. | That is the two gates working as designed; the manifest's `confirmation` is the one that would ask. | Decide whether the **surface** should ask. It is the only thing standing between a member and an irreversible face purge. |
| PH3 | `contracts/apps/photos/sample/` has **no trashed-asset case** — the scripted set purges the one asset it trashes, so the trash shelf is empty in every library case and `purge_in_days` is proved only by the Rust-seeded half. | One extra `media.delete_asset` without its purge. | Take it when the bundle is next regenerated; it moves every id. |

### Agenda and Tasks

| # | Question | Options | Recommendation |
| --- | --- | --- | --- |
| S1 | **Should `schedule.cancel_event` and `schedule.delete_task` carry manifest confirmations?** v0 has none. Both are `Once`/`Medium` and **not parked**, so a non-owner seat can cancel an event or delete a task with no owner-facing prompt. | (a) leave as is, matching v0, and accept that the gate is the risk tier alone; (b) add a manifest `confirmation` to both, which changes what the UI shows before a destructive verb. `delete_event` is the third candidate and the same question covers it. | **(b)** — but it is a product change with a visible consequence, and the lane's evidence for it was "v0 does not have one", which is an observation and not a finding. [D-1020-S6](../decisions.md#wave-4-lane-rulings-1020) is the record of not making it unilaterally. |
| S2 | **The writer half of the `recurrence_tz` fix.** Two Tasks readers asked `schedule_task` for a column it does not have and silently got `undefined`, so **every repeating task fell back to the vault zone regardless of its own**. The readers are fixed at source under [R-1020-35](../decisions.md#decisions--lane-v-1020) with a demonstrated red. Five call sites still **write and assert** `recurrence_tz` into a row shape that has no such column, and the type still declares it optional. | Delete `recurrence_tz` from the Tasks row shape and its writers, or add the column. v0's own schema test asserts the column does **not** exist, so the schema is not in doubt. | **Delete** — but it is v0 blueprint code and not the lane's to remove. Today it is dead weight that reads back as `undefined` the next time somebody trusts it. |
| S3 | **`core_event.rrule_support` has one writer and it is not the app**, and v0 refuses the same rule in one command and accepts it in another. A rule typed into Agenda is stored `'supported'` — the schema default — whatever it actually is; `schedule.add_task` refuses `FREQ=MONTHLY;BYSETPOS=-1` and `schedule.propose_event` stores it. | Should `propose_event` validate the rule (as the port does), and should `rrule_support` be written by the command path at all, or dropped? The column has one writer and **no consumer that changes behaviour on it**. | One question, not two. The port refuses in both commands and records the disagreement as a **named divergence test** rather than reproducing it — reproducing a rule the engine cannot expand would mean shipping a series that is wrong on purpose. |
| S4 | **`queueProviderWriteback` is not ported.** v0's event writes enqueue a writeback to the originating calendar provider. | The connectors plane is on the back burner, so there is no queue to enqueue to. | Named so it is not mistaken for a missed line; it lands with connectors. |

### Tally, Docs, Notes and the wave 2 lanes

| # | Question | Options | Recommendation |
| --- | --- | --- | --- |
| T1 | **v0 labels a foreign-currency expense with the vault's base money.** `loadTally` selects thirteen columns and none is `settlement_currency` or the `rate_*` set, so `ledgerRow` always takes the fallback, `rateSuggestions` can never produce a row, and `export` ships the mislabelling to a file. | One line in v0's projection plus the fixture regeneration that follows it. | **Fix at source** under [R-1020-35](../decisions.md#decisions--lane-v-1020). Wider than one lane because the 29 committed cases are v0's current answers and changing them changes the port's target. |
| T2 | Is **`remove-group-member`** meant to be reachable? Its precondition as written cannot be satisfied. | If yes, the precondition changes; if no, it should not be a manifest action. | Decide. Today it is an action a member can see and never use. |
| DC1 | **The byte door.** `content.read_text` / `content.url` / `content.stage` as `crates/core` messages: one `.proto` message each and three `Request` variants. | Promote them, or leave the app-crate trait as the seam. | **Promote them.** The evidence is lane F's `centraid://` handler answering all three and the Docs test's `StoreDoor` being replaced by the real one, with the same assertions. Then the inline-binary precondition deletes itself and `media.add_asset` stops being `not_implemented`. |
| DC2 | **`enrich_request` on a staged claim.** v0 queues one per device with no contributed derivative; the rows belong to the automations lane's schema. | One call at the end of `promote_staged_blob`. | Take it, from the lane that owns `enrich.*` — writing another lane's table from `core.*` is how two writers of one plane happen. |
| N1 | The **consent pipeline for `crates/search`.** v0's `searchEntity` walks four consent walls; the crate walks none yet and **says so in its README rather than stubbing them**. | An implementation of `Search`, not a change to its callers — the door already takes a `Principal`. | Take it with `crates/vault`'s paged-door work. A stub that looked like a consent check would be worse than an absence a reader can see. |
| N2 | **`contracts/migrations/002_revisions.sql` on the ladder.** Proposed with six tests; the schema still permits a self-parent, a cross-object parent, a re-pointed `parent_revision_id` and a `revises` `core_link`, and **ten** call sites now carry the same reader-side defence. | Land it with the wave's other per-slot migrations. | **Land it.** The migration refuses rather than silently dropping a row that already violates it. |
| N3 | **Two manifest confirmations, re-judged and kept — with a question.** `delete-note` is reversible (a 30-day trash) and is confirm-gated; `restore-note-version` replaces the body a member is looking at and is **not**. | Keep both as v0 has them; gate the restore too; or ungate `delete-notebook`. | **Keep both.** The restore is itself reversible because it appends an occurrence and rewrites nothing, and `delete-notebook` is gated despite only unfiling because a member cannot see from the gesture that their notes survive. Not a silent "deliberate" row. |
| N4 | Two gateway-side findings that change what a member sees: **a trashed note's reference card reads `live`** (`cards.ts` hardcodes `0 AS trashed` for seven types), and **an uncurated entity draws titleless once linked** (`tally.expense` has no card projection, and the powerbox offers expenses as link targets). | Both are gateway-side; neither is a port decision. | Fix both; the two halves of the second disagree by construction. |
| V1 | **Where does an undated photo, or a cancelled task that was never completed, belong in an ordering?** Both are reachable only on the first page today. | (a) the ordering stays; (b) the shelf sorts on a NOT NULL column; (c) ingest guarantees `captured_at` and the column becomes NOT NULL (an ontology change). | **(b) for `tasks.board.logbook`** — a logbook is chronological by when it closed — and **(c) for Photos**, because a library ordered by import date is a different product from one ordered by capture date. |
| V2 | **The seat-side statement assembler needs the same nullable-column refusal.** | Move the check into `pageStatement` with the table facts passed in — which is where the Rust kit puts it — and delete the door's copy. | Take it. One lane over `packages/core` + `packages/client`. |
| V3 | **`ManifestActionEntry.writes`** — declared and never enforced. | Enforce or delete. | Decide; a declaration nothing reads is a comment with a schema. |
| K1 | **The kit's year-3 axis** needs a settlement's `core_transaction` and an edited expense's `core_entity_revision` rows, so two of the ten measured Tally numbers are round because the plane is empty. | Both tables belong to planes other than Tally's. | The finance and revision lanes state the row shapes. |

---

## Related

- [docs/release.md](../release.md) — the release pipeline, the VPS run, the egress ratchet
- [docs/enrollment.md](../enrollment.md) — signing identities and store enrolment
- [docs/decisions.md](../decisions.md#v1-platform--rust-core-kmp-shell-electron-seat-gateway-anywhere-1020) — the rulings each of these turns on
- [TESTING.md](../../TESTING.md#the-v1-gate-profiles-1020) — what each gate profile proves, and where it stops
- [`receipts/issue-1020-v1-platform.md`](../../receipts/issue-1020-v1-platform.md) — the evidence layer: where each of these was found, and the demonstrated red behind it
