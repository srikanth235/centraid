---
summary: 'An app is code (handlers, migrations, assets) in the gateway git store, plus a stable per-app dir holding its data, conversations, and attachments.'
title: 'Apps'
---

# Apps

A Centraid app is a folder of code, plus the data and history that accumulate as it's used.

The **source tree** is what you author and upload:

```
my-app/
  app.json                    # the manifest (required)
  index.html
  app.css, app.js, …          # static assets — see allowlist
  queries/<name>.js           # dispatched by centraid_read
  actions/<name>.js           # dispatched by centraid_write
  migrations/<NNNN>_<name>.sql
  automations/<id>/           # optional — each automation is a subdir
    automation.json           #   manifest (triggers, prompt, requires)
    handler.js                #   the JS the runtime runs when the trigger fires
```

The **mounted tree** is the app's persistent per-app dir on the gateway:

```
<appsDir>/<id>/
  data.sqlite                 # app-owned (handler) data, never served as a static file
  runtime.sqlite              # conversation ledger + automation state KV
  blobs/<hash>                # content-addressed attachment bytes
```

App **code** is no longer stored under this dir. The gateway owns a git store and resolves an app's live code dir through a host-injected `codeDirOverride` (issue #137) — the materialized `main` worktree — instead of a local `versions/` snapshot. The per-app dir above holds only the app's **state** (data, conversations, attachments), which is what persists across code changes. (Older builds extracted uploads to `<appsDir>/<id>/versions/v_…/` and pointed at the active one via `current.json`; that path is legacy.)

No app server framework. No build step required at runtime (you can author in TypeScript and ship compiled `.js` next to it, but the gateway loads `.js` only).

## What an app _is_

- **A unit of code.** App code lives in the gateway's git store; the runtime resolves the live code dir per app id (issue #137). Each app is one registry entry in `<appsDir>/_registry.json`.
- **A unit of data isolation.** Every app gets its own `data.sqlite` (handler-owned data) and `runtime.sqlite` (conversation ledger + automation state). Handlers get a scoped DB proxy — there is no path from one app's worker to another app's databases.
- **A unit of identity.** App ids are the gateway's primary key. They appear in URLs (`/centraid/<id>/...`), in tool calls (`{ app: "<id>", ... }`), and as the per-app key the runtime resolves `runtime.sqlite` / `data.sqlite` / `blobs/` by.
- **A tool catalog.** An app's `queries[]` and `actions[]` arrays in `app.json` _are_ the app's tool surface — each entry is a tool with a name, description, input/output schemas, and side-effect annotations. Your UI, an AI agent, and the CLI all reach those tools through the same dispatcher.
- **A unit of routing.** The runtime knows about apps by registry id and dispatches calls to the app's live code.

## The manifest (`app.json`)

`app.json` is the only required file. Everything else is optional, but a useful app has at least one query or action.

```json
{
  "manifestVersion": 1,
  "id": "hydrate",
  "name": "Hydrate",
  "description": "Track 8 cups a day.",
  "version": "0.1.0",
  "tables": [...],
  "actions": [
    {
      "name": "set-cups",
      "description": "Set today's cup count (clamped 0..8).",
      "confirmation": "none",
      "input": { "type": "object", "properties": {...}, "required": ["cups"] },
      "output": { "type": "object", "properties": {...} },
      "writes": ["hydrate_daily"]
    }
  ],
  "queries": [
    {
      "name": "get-today",
      "input": { "type": "object", "properties": {} },
      "output": { "type": "object", "properties": {...} },
      "reads": ["hydrate_daily"]
    }
  ],
  "knobs": [...]
}
```

Key fields:

- `id` — must match the folder name and the URL slug.
- `actions[]` / `queries[]` — each entry describes a handler. The dispatcher uses `input` (JSON Schema draft 2020-12) to validate calls before the handler runs; `reads`/`writes` document which tables the handler touches.
- `tables[]` — declared shape. The actual schema lives in `migrations/`; `tables[]` is for tooling and agents.
- `knobs[]` — user-facing settings the desktop shell renders. Each knob has a `key`, `label`, `type` (`segmented`, `swatch`, …), `default`, and per-type options.

See [Build → app.json](/build/app-json) for the full schema.

## Code versus state

Code is versioned in the gateway's git store. State — `data.sqlite`, `runtime.sqlite`, `blobs/` — is **not** versioned; it persists across every code change, in the stable per-app dir.

The runtime resolves an app's live code dir lazily through the host-injected `codeDirOverride`, while `entry.path` (the registry's per-app dir) always names where state lives. That clean split between git-owned code and a stable per-app data dir is the whole point: reverting code never touches the data, conversations, or attachments that accumulated under it.

> The git-store publish/version surface (publishing, version history, activation) lives in `@centraid/gateway`, not in app-engine's `/centraid/*` routes. See the gateway and build docs for that surface.

## App ids — what's reserved

Ids beginning with an underscore are reserved for the gateway's own routes (`_apps`, `_tool`, `_turn`, `_changes`). Trying to register `_anything` returns a `RegistryError`. App ids are a plain filesystem-safe slug — alnum, `-`, `_`; no path separators, no dots. (Automation apps used to carry a dotted `auto.` prefix; that distinction moved to the manifest's `kind` field, so the id grammar is a plain slug again.)

## Where to go next

- [Queries and actions](/concepts/queries-and-actions) — what handlers actually do.
- [Change stream](/concepts/change-stream) — how the UI knows when data moved.
- [Automations](/automations/index) — triggered work that lives alongside apps.
- [Build → App anatomy](/build/app-anatomy) — practical walkthrough.
