---
summary: 'Automations are Centraid apps that fire on a trigger — scheduled, webhook-driven, or manual — instead of waiting for a UI click. Each lives as a directory inside its owning app.'
title: 'Automations'
---

# Automations

An **automation** is an app-owned background job that fires on a **trigger** instead of a UI click. The trigger is a cron schedule, an inbound webhook POST, or an explicit "Run now." When it fires, a JS handler runs in a worker thread with a curated `ctx` surface — tools, a constrained agent turn, KV state, and prior-run history.

## Where they live

Every automation lives inside an app folder:

```
<appCodeDir>/automations/<id>/
  automation.json    # manifest — source of truth
  handler.js         # generated handler — what actually runs
```

The directory **is** the automation. There is no separate registration step; the manifest is the only thing the scheduler needs to register the trigger.

## Two structural shapes, same runtime

| Shape                        | Example                                             | Description                                                                                                                                       |
| ---------------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| UI app that owns automations | `hydrate/` with `automations/weekly-encouragement/` | A regular app with `index.html` + queries + actions, _and_ automations operating on its data.                                                     |
| Headless automation app      | `briefing/`                                         | An app folder whose only purpose is automations — no `index.html`, empty `actions[]` / `queries[]`. Its `app.json` declares `kind: "automation"`. |

A headless automation app is marked by **`kind: "automation"` in its `app.json`** — not by any id prefix. (An older `auto.` id-prefix convention was retired; `scaffoldAppFiles` now writes a plain-slug id plus `kind: "automation"`.) The desktop gallery reads `kind` to render the app in the **Automations** section instead of among UI apps.

## Three trigger kinds

`triggers` is an array on the manifest:

| Kind                    | Shape                                 | Notes                                                                                                                                  |
| ----------------------- | ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `cron`                  | `{ kind: "cron", expr: "<5-field>" }` | Standard 5-field cron, matched against the gateway host's **local wall clock**. Numeric fields only. Multiple per automation allowed.  |
| `webhook` (provisioned) | `{ kind: "webhook", id, secretHash }` | Fires on POST to `/_centraid-hook/<id>` on a remote gateway. At most one webhook per automation. Desktop never registers the listener. |
| `webhook` (pending)     | `{ kind: "webhook", pending: true }`  | The conversational builder's handoff form — a privileged server pass mints the route slug + secret.                                    |

An empty `triggers: []` (or omitting the field) is legal — the automation fires only via "Run now".

## What the handler can do

```js
export default async ({ ctx, log }) => {
  // ctx.tool(name, args)     — call an MCP tool (including centraid_read/write)
  // ctx.agent({ prompt })    — one constrained model turn (the only billed rail)
  // ctx.state.get/set/delete — cross-run KV in runtime.sqlite
  // ctx.runs.last/list       — prior-run history
  // ctx.input                — the trigger payload (webhook body, onFailure context)
  return {
    summary: 'short line for the run list',
    output: {
      /* … */
    },
  };
};
```

Automations do **not** get the `db` proxy that queries and actions receive. To read or write data — including data in their own owning app — they go through `ctx.tool('centraid_read', …)` and `ctx.tool('centraid_write', …)`. An automation is a client of the [three-tool dispatcher](/reference/three-tool-dispatcher), same as a UI iframe.

## Run audit

Every fire writes to `<appDir>/runtime.sqlite` using the shared conversation ledger (`conversation ⊃ turn ⊃ item`):

- Each fire is its **own `conversations` row** (a fresh id, `kind: "automation"`, tagged with the automation ref). An automation's run history is the set of conversations sharing that ref.
- One `turns` row per fire — status, summary, output, the trigger kind + origin, error if any. (The fire **is** the turn.)
- One `items` row per `ctx.tool` / `ctx.agent` call — ordered, with timings and full request/response — plus a `message_in` item (ordinal 0) holding the trigger input.

`history.keep` on the manifest controls retention — `{count: N}`, `{days: N}`, `"all"`, or `"errors"`. Default is `{count: 100}`.

## Why this shape

A handful of decisions hold the design together:

- **Disk-first.** The manifest is the source of truth. No SQLite definition table; toggling `enabled` is a file write. A scheduler that reads the directory has everything it needs.
- **Handler is generated JS, not an agent loop.** The conversational builder turns intent into code once; the runtime runs that code. The `prompt` field is documentation, not execution.
- **Deterministic, replay-safe handlers.** A crashed fire re-runs from the top — there is no resume journal — so a handler must avoid ambient nondeterminism (`Date.now()`, `Math.random()`, raw `fetch`/`fs`). All effects go through the audited `ctx.*` rails. An authoring-time lint (`lintHandlerSource`) rejects the offenders at publish time.
- **Webhook credentials are privileged.** The builder agent can declare intent (`pending: true`) but cannot mint the route slug or secret — those require crypto-random generation and one-shot user surfacing. A server pass provisions.
- **Per-app run ledger.** `runtime.sqlite` is separate from `data.sqlite`, so automation churn doesn't pollute the app's user data.
- **Three-tool dispatcher all the way down.** An automation reaches its parent app's data the same way any other client does. No shortcut, no separate API.

## Where to go next

- [Automations → Overview](/automations/index) — the full tab, top to bottom.
- [Manifest reference](/automations/manifest) — every field of `automation.json`.
- [Triggers](/automations/triggers) — cron, webhook, multi-trigger composition.
- [Handler runtime](/automations/handler) — what `ctx.*` actually does.
- [Webhooks](/automations/webhooks) — the provisioning handshake.
- [Run history](/automations/run-history) — the conversation/turn/item ledger, retention.
- [Conversational builder](/automations/conversational-builder) — how the agent scaffolds an automation.
