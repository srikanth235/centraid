# The vault ontology

The ontology is the crux of Centraid: one person-owned model of a life, over which every app is a projection and through which every agent acts. This document is its **current state** — what the model is today, which of its commitments are enforced by code, and the register of places where the implementation and the design disagreed. The design itself is the published page at [centraid.dev/docs/ontology](https://centraid.dev/docs/ontology/), authored in [`scripts/docs-site/src/content/ontology-body.html`](../scripts/docs-site/src/content/ontology-body.html); this file is the engineering companion that keeps that page honest.

Vocabulary is [glossary.md](glossary.md); rulings are [decisions.md](decisions.md) (search "Ontology v0 close (#916)", "Ontology reconciliation (#883)", "Grants v2", "Portable export custody"). History lives in receipts and issues, not here: the ontology package landed in [#252](https://github.com/srikanth235/centraid/issues/252), rule 10 in [#274](https://github.com/srikanth235/centraid/issues/274), the first audit in [#441](https://github.com/srikanth235/centraid/issues/441), the reconciliation in [#883](https://github.com/srikanth235/centraid/issues/883), and the v0 close in [#916](https://github.com/srikanth235/centraid/issues/916).

## Where the truth lives

| Question | Answer | Source |
| --- | --- | --- |
| What tables exist, what they are called, and what each row's life may be | The entity registry — an allow-list before it is a name table. Each entity declares its logical name, its physical table, its label, its lifecycle (`append-only` / `mutable` / `trash` / `machinery`), whether it is a _projection_ of a parent, and the deletion role of every reference onto a person or a content item. | `entities` in [`contracts/schema/v0-registries.json`](../contracts/schema/v0-registries.json), embedded by [`crates/ontology/src/registries.rs`](../crates/ontology/src/registries.rs) |
| Which physical tables are deliberately **not** registered, and which never leave the gateway | `localTables` (the supertype index, blob custody internals, the replica plane, the locker's own unlock credential) and `privateTables`, one list each | the same file |
| What each table looks like | The forward-only ladder: rung one is the baseline, rung two the revision-graph guards. The baseline file is both the migration the binary embeds and the fixture ([D-1020-D1-13](https://github.com/srikanth235/centraid/issues/1020)). | [`contracts/migrations/`](../contracts/migrations/001_baseline.sql), ladder in [`crates/vault/src/migrations.rs`](../crates/vault/src/migrations.rs) |
| Which schemas are life data and which are plumbing | `ontologyPacks` versus `machineryBands` — explicit, so a new schema fails loud rather than mis-shelving | `contracts/schema/v0-registries.json` |
| Where every `(type, id)` pointer is | Read from the schema, not from a list: every pointer pair is a composite foreign key into `core_entity(entity_type, entity_id)`, so `PRAGMA foreign_key_list` is the pointer set. The one pointer no `REFERENCES` clause can express — `share_authority.principal_id` under `principal_kind = 'person'` — is named beside the merge that must sweep it. | `core.merge_party` in [`crates/vault/src/commands/core.rs`](../crates/vault/src/commands/core.rs) |
| What the design says | The published page: thesis, entity map, §03 schema, principles, layer stack, ownership matrix, gateway contract, the ten rules | [`ontology-body.html`](../scripts/docs-site/src/content/ontology-body.html) |
| Whether the page and the DDL agree | Nothing compares them in the current tree — see ONT-01 | — |
| The version | `ONTOLOGY_VERSION` (**1.0**), stamped on `agent_command` contracts only; the command gate refuses a contract on any other version. The file's own version is `PRAGMA user_version`, which counts the v1 ladder's rungs (2), beside `PRAGMA application_id = CEN1`. No row carries a version. | [`crates/vault/src/commands/mod.rs`](../crates/vault/src/commands/mod.rs), `crates/vault/src/migrations.rs` |

The registries were transcribed from the v0 TypeScript schema before [#1020](https://github.com/srikanth235/centraid/issues/1020) removed that tree; the exporter left with it, so `contracts/schema/v0-registries.json` is committed data with no generator (its `$generatedBy` still names the deleted tool), and the Rust crates read it rather than keeping a second copy of "which tables never leave the gateway". [`crates/ontology`](../crates/ontology/README.md) opens and verifies a file — the two frozen corpora under `contracts/golden/` (`issue-929` and `issue-1020`, the ends of the `user_version` window it accepts), `contracts/schema/vault-ddl.sql` as the corpus schema rendered for reading, and the doctor. [`crates/vault`](../crates/vault/README.md) founds, migrates and writes. Their tests are `crates/ontology/tests/golden_vault.rs` (the frozen files open and every digest the release froze is reproduced), `crates/ontology/tests/commitments.rs` (one test per row of _Commitments the code enforces_ that a file-level check can hold), `crates/ontology/tests/fixtures.rs` (the DDL fixture is regenerated and diffed) and `crates/vault/tests/baseline.rs` (a founded file carries exactly the corpus schema).

## One file, three bands

A vault is **one SQLite file**, `vault.db`, holding three bands ([#916](https://github.com/srikanth235/centraid/issues/916)):

- **The model** — the `core` spine, seven life-domain packs, the `access` and `agent` planes, and the machinery bands (`share`, `sync`, `enrich`, `outbox`, `notifications`, `blob`).
- **The audit band** (`auditBand` in the registries; the writer is `crates/vault/src/audit.rs`) — `access_provenance`, `access_receipt`, `agent_command_invocation`, `agent_invocation_check`, `agent_evidence`, `agent_explanation`, and the archive manifests. Append-only **by trigger**: UPDATE and DELETE are refused by the engine, not by convention.
- **The ledger band** (`crates/vault/src/ledger/`) — the conversation transcript (`conversation ⊃ turn ⊃ item`), automation state, and the archive/digest rows that bound it.

The audit stream lived in a second file, `journal.db`, until #916. Splitting it was a size argument and it cost correctness: a receipt in another file is not atomic with the mutation it describes, and every pointer across the seam had to be a gateway promise instead of a foreign key. One file gives one ACID boundary — invocation, rows and receipt commit together — and makes `core_entity_revision.invocation_id` a real key. The size argument is answered where it belongs: each band declares a retention window in one place (`RETENTION_WINDOWS`: audit 365 days, ledger 90), and the archival pass proves custody of what it sealed before it prunes. Both bands are excluded from the portable export and the device replica **by band** (`privateTables` in the registries), so there is no second list to maintain.

One file also collapsed the backup protocol. A snapshot manifest now carries **exactly one `db` entry** (`vault.db`, with its content hash, WAL generation and base tick), and the WAL stream is addressed by generation: one **tick marker** per tick at `wal/tick/{generation}/{tick}`, recording the position the segment stream reached. The two-database coordination — the capture order, the coordinated break, the pair marker and the two-base same-tick refusal — is deleted, because a pair is what it existed to keep in step. Restore still replays to the newest marker the listing can _prove_ it reached, and no marker still means the base floor, so an idle stream stays distinguishable from a lost tail. The implementation is `crates/vault/src/backup/`, with its format fixture at `contracts/golden/format-golden.json`.

## The shape today

A freshly founded vault, from `contracts/migrations/001_baseline.sql`: **139 base tables** (plus 18 FTS virtual tables and their shadow tables), 244 indexes, 268 triggers, 1 view; rung two adds one guard table and four triggers. **96 tables are registered entities**; the rest are declared local, band-declared, or FTS.

| Schema | Kind | Tables | What it holds | Bundled consumer |
| --- | --- | --- | --- | --- |
| `core` | spine | 20 registered (+ `core_entity`, `core_entity_kind` local) | vault, party, identifier, place, event, account, transaction, content item, derivative, document, attachment, activity, link, anchor, concept scheme, concept, tag, collection, collection entry, entity revision | every app |
| `schedule` | pack | 8 | calendar, event extension, attendee, task, project, section, recurrence exception, exception attendee | Agenda, Tasks |
| `social` | pack | 6 | contact channel, circle, circle member, thread, participant, message | People (channels), Tally and sharing (circles); **threads and messages have no surface, but do have writers** |
| `knowledge` | pack | 2 | note, annotation | Notes; annotations are the memo mechanism everywhere |
| `media` | pack | 6 | asset, face region, phash, memory, memory member, face cluster | Photos |
| `people` | pack | 2 | profile, important date | People |
| `locker` | pack | 5 registered (+ `locker_auth_credential` local by ruling) | item, address, alias, field, passkey | Locker |
| `tally` | pack | 12 | friend, group, expense, split, recurring split, payer, line item, line allocation, recurring expense, settlement, obligation, nudge | Tally |
| `access` | plane | 5 | app, agent, app ext, device, seed row | the gateway |
| `agent` | plane | 2 | command, capability | the gateway |
| `audit` | band | 8 (band-declared) | provenance, receipt, invocation, check, evidence, explanation, archive manifest, archive pass | the gateway |
| `ledger` | band | 14 (band-declared) | conversations, turns, items, attachments, harness sessions, locks, workspace selection, health, provider consent, automation state and cursors, trigger ingress, archive, digest | app-engine |
| `share` | machinery | 8 | authority, pending asks, last-used stamps, delivery config, fulfillment, party↔vault binding, and the subscription seat's own pair — subscription and subscription lineage ([#929](https://github.com/srikanth235/centraid/issues/929)) | the grant plane |
| `sync` | machinery | 8 | connection, credential, cursor, run, health, external id, import batch, import row | connectors |
| `enrich` | machinery | 5 | request, policy, policy rule, derivation, embedding | recognition automations |
| `outbox`, `notifications`, `blob` | machinery | 1 + 1 + 2 registered (+ 11 blob tables local) | outbox items; notices; custody state and rollup | the gateway |

Domains the design carried and the implementation dropped, each for having **no consumer**: `home` and `business` in [#883](https://github.com/srikanth235/centraid/issues/883) (ruling O-domains; product case in the closed proposal [#885](https://github.com/srikanth235/centraid/issues/885)), then `health`, `finance`, the observation spine, the learn loop (`agent.correction` / `agent.judgment`), `consent.app_view`, `consent.export_job` and `schedule.availability_rule` in #916 (ruling ONT-06).

**Lifecycle, as declared:** 13 trash, 10 append-only, 40 mutable, 33 machinery. **Projections** (a part of an entity, keyed by its parent, no supertype row): 12.

## Derived data is not automatically local data

A tempting rule after [#1014](https://github.com/srikanth235/centraid/issues/1014) (B10) was "derived data does not replicate": enrichment writes 512-float vectors and a provenance stamp per derivation, and on a 50k library that is what a seat's change log fills up with.

The rule is wrong, and the reason is [R1](decisions.md#one-vault-every-seat-996): a seat holds `vault.db` WHOLE. There are no row filters and no field masks, so a table travels or it does not — and a replicated table may never reference a private one ([R4](decisions.md#one-vault-every-seat-996)), which is a constraint on the _graph_, not on how the row was produced.

So the question that decides it is not "was this derived?" but:

1. **Does a seat read it?** `media_face_cluster` is derived and the People page walks it; `media_asset_phash` is derived and the library page reads it. Both travel.
2. **Does a replicated row point at it?** `core_content_text.derivation_id` REFERENCES `enrich_derivation`, so making that stamp private would break the property the private list exists for. It travels.
3. **Neither?** Then it is gateway machinery: private, and usually unregistered too. `enrich_request` is private (the queue is work, not data) and `enrich_target_failure` is both private and unregistered — a restore re-derives one host's failure counts by simply attempting the work again.

Each derived table's answer is its membership, or not, in `privateTables`; `crates/ontology/tests/commitments.rs` (`no_replicated_table_references_a_private_one_by_name`) holds the graph half of the rule.

## Commitments the code enforces

Each row is a design commitment, where it is enforced, and the test that goes red when it is broken. A commitment with no row here is a wish, not a rule.

| Commitment | Mechanism | Enforced by |
| --- | --- | --- |
| Every physical table is either a registered entity, a declared local one, or a private one | `entities`, `localTables`, `privateTables` compared against `sqlite_master` | `commitments.rs` `every_physical_table_is_a_registered_entity_a_local_table_or_a_private_one` |
| Every entity row has an id unique across the model, and every `(type, id)` pointer resolves | the `core_entity` supertype: membership triggers per entity table, composite `REFERENCES core_entity(entity_type, entity_id) ON DELETE CASCADE` on every pointer pair | `commitments.rs` `every_ontology_entity_table_carries_its_membership_triggers` |
| Every row states what its life may be, and the schema matches | per-entity `lifecycle`; trash ⇒ the `deleted_at`/`purge_at` pair with its CHECK | `commitments.rs` `every_trash_entity_carries_the_reversible_delete_pair`. The mutable ⇒ `updated_at` + touch trigger half is **not held** by a current test. |
| The declared bands and packs cover the registry | `ontologyPacks` / `machineryBands` | `commitments.rs` `the_declared_bands_and_packs_partition_the_registry` |
| Evidence cannot be rewritten, and never leaves the machine | audit-band triggers refusing UPDATE/DELETE; band exclusion from export and replica | `commitments.rs` `the_audit_band_refuses_an_update_and_a_delete`; `privateTables` |
| A replicated row never references a private one | the private list is a constraint on the graph | `commitments.rs` `no_replicated_table_references_a_private_one_by_name` |
| A receipt and the mutation it describes commit together | one file, one transaction, receipt written inside the command gate | `crates/vault/src/audit.rs`, `crates/vault/tests/commands.rs` `a_real_command_writes_its_row_its_log_row_and_its_receipt` |
| The ledger band stays bounded without losing history | seal a cold turn range into the CAS, then prune, as two commits in that order | `crates/vault/src/ledger/archive.rs`. The audit band's archive pass has **no v1 implementation**; `retentionWindows` still declares both. |
| Secrets are ciphertext at rest, never indexed, revealed only with a receipt | `sealedColumns` in the registries; the search door refuses to project a sealed column | `crates/vault/src/custody/seal.rs`, `commitments.rs` `no_sealed_column_appears_in_any_fts_ddl`, `crates/search/src/domains.rs` `assert_no_sealed_column` |
| A portable export never carries a key in the clear | the bundle holds ciphertext; keys leave only inside the password-wrapped `custody/recovery-kit.json` | `crates/centraid/src/cmd/export.rs`. Re-sealing under a target's own key on import has **no v1 implementation**. |
| Delete is a reversible trash with a grace window, and a purge is one act | the `deleted_at`/`purge_at` pair; purge is a typed command whose DELETE the supertype cascades | per-pack DDL, `purge_*` commands in `crates/vault/src/commands/` |
| A person can be purged or merged, and money and authority refuse it while they still name them | `core_party` trash pair + FK delete rules per column (no delete action — so the engine refuses — on money and authority, `SET NULL` on attribution); `core.merge_party` re-points every reference, generated from `PRAGMA foreign_key_list` | `crates/vault/src/commands/core.rs`, `crates/vault/tests/people_commands.rs` |
| Every reference onto a person or a content item declares WHY deleting the parent does what it does | `deletionRoles` on each entity — owned child, derived, attribution, participation, durable record ([R22](decisions.md#one-vault-every-seat-996)) | declared in the registries; the per-role purge behaviour is **not held** by a current test |
| Every row change of a replicated table reaches every seat | SQLite **session capture** on the gateway's own connection, one session per table, writing a full row image per (table, primary key) per commit into `replica_log`; ONE log ([#1014](https://github.com/srikanth235/centraid/issues/1014) R-1014-1), pruned by the retention pass alone | `crates/vault/src/log/capture.rs`, `crates/vault/src/log/store.rs`, `crates/vault/tests/log_plane.rs` |
| Every write to a replicated table runs inside the commit pair, on the connection that makes it | `Vault::commit` is the only writable connection; `Vault::read` sets `PRAGMA query_only`; sessions are abandoned before a ROLLBACK | `crates/vault/src/log/guard.rs`, `log_plane.rs` `a_write_through_the_guard_reaches_the_log_and_nothing_else_can_write` |
| A member's edit is refused when the row moved under it, and every replicated mutable row can say so | `row_version` on the row plus its touch trigger, bumped in the SAME trigger as `updated_at` | `commitments.rs` `every_replicated_mutable_entity_table_carries_row_version` |
| Bytes are rented, rows are owned: a content item with no live reference is reclaimable | `contentReferences` enumerates every renting column | the registries; read by `crates/vault/src/commands/media.rs` and `crates/vault/src/backup/restore.rs` |
| Money states its currency | `currency` on `tally_group`, `tally_expense` and `tally_settlement`, with triggers holding a grouped row to its group; `Money` is minor units plus an ISO 4217 code | `commitments.rs` `money_states_its_currency`, `crates/apps/kit/src/money.rs` |
| Recurrence exceptions survive a series edit | keyed on `original_start_local` + `recurrence_semantics`, not a resolved UTC instant; attendee overrides are rows | `crates/vault/src/time/occurrence.rs` |
| An unsupported RRULE is refused, never silently reinterpreted | `assert_supported` at the write boundary, `inspect` where a surface can report, `parse` answering `None` on a read | `crates/vault/src/time/rrule.rs` |
| A vault a harness touched is still sound | page integrity, engine FKs, dangling receipts and the seal-key fingerprint | `crates/ontology/src/doctor.rs`, `restore_check` in `crates/vault/src/backup/restore.rs`, `centraid doctor` |
| A vault from the frozen corpus opens and keeps every row | the corpora under `contracts/golden/` | `crates/ontology/tests/golden_vault.rs` |
| A schema change is a new rung (**ONT-ladder**) | forward-only ladder; a rung is appended, never inserted, and never edited once released; a file above the binary is `DowngradeRefused` | `crates/vault/src/migrations.rs`, `crates/vault/tests/baseline.rs`, `crates/vault/tests/revisions_migration.rs` |
| One judgment, one mechanism | stars are a flags-scheme `core.tag`, memos are `knowledge.annotation`, employment is a `core.link` — with **no** mirror column anywhere | the baseline DDL; `set_starred` in `crates/vault/src/commands/locker.rs` |
| A command speaks the vault's ontology version | equality on `agent_command.ontology_version` at invocation | `crates/vault/src/commands/mod.rs` |
| Nothing outside the confined crates writes vault SQL | `sql-confinement`: SQL literals live only under `crates/{ontology,vault,seat,search}` and `crates/apps/kit`, tests included | `cargo xtask rules` |
| The published page describes this schema | — | **not held** — see ONT-01 |

## The ten rules, as implemented

The page's §11 states ten hard rules. This is where each stands in code.

| Rule | Standing |
| --- | --- |
| R1 IDs immutable, meaningless | Held, and now unique across the model: every entity row is a `core_entity` row, so an id names one thing. `core.merge_party` folds one party into another: every reference re-points — the sweep is generated from `PRAGMA foreign_key_list`, never a hand-kept list — and the merged party's row is deleted (`crates/vault/src/commands/core.rs`). A cross-source match the owner has not accepted stays two rows with a `same-as` link. |
| R2 Extend, don't fork | Held for every shipped domain: `people_profile`, `tally_friend`, `media_asset`, `schedule_event_ext` anchor a core row by UNIQUE FK. **Locker is the one written exemption** — an owner-only secret anchors nothing in core by design (its DDL header). |
| R3 Relationships typed and temporal | Held for cross-entity meaning (`core.link`). Junction tables that carry state (`schedule_attendee.partstat`, `social_circle_member.capability`, `tally_expense_split.share_minor`, `core_collection_entry.position`) are typed rows, not the boolean junctions the rule forbids. |
| R4 Writes are typed commands | Held. Every write is a registered command in `crates/vault/src/commands`, run through one gate order; there is no generic row writer. A SQL literal outside the confined crates is a `sql-confinement` failure. |
| R5 Durable facts vs rebuildable projections | Held with two registered stored projections: `social_thread.last_message_at` (healed by sweep) and `blob_custody_*` (rebuilt). Tally stores no balance. The `media_asset.favorite` mirror is gone. |
| R6 Consent travels with the data | Held; the authority answers live in one plane (`share.authority`, [#883](https://github.com/srikanth235/centraid/issues/883)), and the plane that answers "may this actor reach this data" is named `access`. The principal kinds the plane admits are `person`, `circle`, `harness`, `device` and — accepted in [#928](https://github.com/srikanth235/centraid/issues/928) wave 1, written in wave 3 — `automation`, whose id is a manifest ref (`<app_id>/<automation_id>`) and whose subject is a command pack (`agent.pack`) or an entity type (`core.entity`), read or act. `app` is not among them and is not reserved as a value: first-party apps are the owner's own screens, not principals. A portable export carries policy and ciphertext, never a key. |
| R7 Version the ontology explicitly | **Held, restated.** The version is a property of the file (`user_version`) and of the command contract (`agent_command.ontology_version`) — not of a row (ONT-04). |
| R8 Lifecycle is declared, not adopted | **Held.** This rule replaced "corrections become durable judgment", whose loop had DDL, commands and no caller (ONT-06). Each entity declares its lifecycle and a test holds the schema to it. |
| R9 (rule 10) One judgment, one mechanism | **Held with no exception.** The single registered mirror (`media.asset.favorite`) was deleted; Photos' star anchors on `media.asset`, Docs' on `core.document`. |
| R10 Every app rides the paved road | Held: manifests become grants, reads are scoped, writes are commands, app-declared tables are an `ext` block in the manifest, which no bundled app uses. The registered-view half of the road left with the views nothing registered (ONT-06). |

## Drift register

The do-not-fix-quietly register for the ontology. Each row names the finding, the evidence, and its standing. A row is removed when its finding is gone from the code **and** nothing about the removal is worth remembering; it is kept as **fixed** when the mechanism that closed it is the thing a future reader needs. Nothing here is "deliberate" any more: [#916](https://github.com/srikanth235/centraid/issues/916) re-judged every deferred row on its merits and three of them — polymorphic references, the favorite mirror, the consent plane — had been filed as settled on nothing but a prior ruling.

**Reader-side drift against a landed ruling** is a category of its own, introduced by [#996](https://github.com/srikanth235/centraid/issues/996) ([R22](decisions.md#one-vault-every-seat-996)): a ruling the storage layer enforces — a column, a CHECK, a trigger, a dropped table — while a reader ignores it, reads a column the writer does not write, or aggregates away the thing the column was added to carry. It looks fixed from the DDL and from the schema tests, which is exactly what makes it drift rather than a bug: the mechanism the register asks a **fixed** row to name is present, and the property is still absent at the surface the owner sees. **A storage ruling is not enforced until a reader test holds it** — one test that writes through a real command and reads through a real app query, across two app surfaces. Three of [#916](https://github.com/srikanth235/centraid/issues/916)'s rulings were in this state when #996 audited them (ONT-22, ONT-23, ONT-25), and each row below in this category names the ruling it drifted from.

| Id | Finding | Standing |
| --- | --- | --- |
| **ONT-01** | The published page described a model eleven rungs behind the vault, and nothing compared the two. | **open.** A TypeScript parity test held §03 equal to the live DDL until [#1020](https://github.com/srikanth235/centraid/issues/1020) removed the tree it lived in, and nothing in the current tree compares `ontology-body.html` with `contracts/migrations/`. The page is `ontology v1.0`. |
| **ONT-03** | `media_asset.favorite` — a boolean on a domain table, the exact shape rule 10 calls a rejected review — was kept as a "single-writer mirror" for the Photos replica shape ([#419](https://github.com/srikanth235/centraid/issues/419), [#441](https://github.com/srikanth235/centraid/issues/441)). | **fixed** — the column is gone. A mirror is two truths and a postcondition asserting they agree is a test for a problem that need not exist. The star is a flags-scheme tag anchored on `media.asset`, the entity Photos shows; Docs anchors on `core.document`. Supersedes the #419/#441 mirror ruling. |
| **ONT-04** | R7 said every row stamps `ontology_version`; two tables carried the column and the gateway checked it on command contracts alone. | **fixed** — one version scheme: file + contract. `core_party.ontology_version` dropped; `ONTOLOGY_VERSION` is `"1.0"` (`crates/vault/src/commands/mod.rs`) |
| **ONT-05** | Two "owner" identities: the ontology anchored authority on parties, the gateway's owner model ([#726](https://github.com/srikanth235/centraid/issues/726)) says a party never confers authority. Nothing named the seam. | **fixed** — `core_vault.owner_party_id` → `self_party_id`, the vault's own party as the person-as-**data**. Authority stays gateway-side. The page and the glossary say so. |
| **ONT-06** | Dormant mechanisms with registered DDL and no producer or consumer: `health.*`, `finance.*`, `core.observation`, `consent.app_view`, `consent.export_job`, `agent.correction`/`judgment`, `schedule.availability_rule`, `social.thread`/`message`. | **fixed** — O-domains' test applied evenly. All of the above left the model **except** `social.thread`/`message`, which have writers. `consent.policy` kept `retention`/`purpose`/`minimization` and dropped `residency`. Intent lives in issues, never in dormant DDL. |
| **ONT-07** | `consent_app.origin` still admitted `'generated'`, unwritten since [#799](https://github.com/srikanth235/centraid/issues/799). | **fixed** — `CHECK (origin IN ('installed'))`. The same sweep removed `core_vault.status 'exported'`, `access_app.status 'suspended'` and `agent_capability.verb 'learn'`: CHECK values that were fiction, not vocabulary. |
| **ONT-08** | Lifecycle by touch: 12 tables with the trash pair, ~40 mutable tables without `updated_at`, and nothing saying which tables were append-only by design. | **fixed** — every entity declares `append-only` / `mutable` / `trash` / `machinery`; `created_at`/`updated_at` have one shape (`NOT NULL DEFAULT` + touch trigger) across packs and bands alike. The ledger band is the one exemption (its clock is engine epoch-ms). The declaration is in the registries; `commitments.rs` holds the trash half, and the mutable half has no current test (see the commitments table). |
| **ONT-09** | Five vocabularies for one polymorphic pointer (`target_`, `subject_`, `entity_`, `item_`, `from_`/`to_`), previously ruled survivable through the registry. | **fixed** — a pointer pair is `target_type` / `target_id`, each a composite foreign key into `core_entity`; `from_`/`to_` survives only where the pair genuinely has two ends (`core.link`). The registry-makes-it-survivable ruling was exactly the deference #916 had to reopen. |
| **ONT-10** | Recurrence encoded two ways: RRULE text and `weekday_mask` (availability). | **removed** — the availability rule left with ONT-06, so there is one encoding. The RRULE subset is now explicit: unsupported parts are **refused** at the boundary rather than silently dropped (`crates/vault/src/time/rrule.rs`), which is what made the second encoding tempting. |
| **ONT-11** | `core_party.kind` admits `animal` and the page did not say so. | **fixed** on the page |
| **ONT-12** | Search coverage by touch: album, notebook, project, group and place names unindexed. | **fixed** — 18 FTS indexes, including the collections, projects, circles and places that were missing, and `deletedColumn` on `tally.expense` and `people.profile` so a trashed row leaves search too |
| **ONT-13** | `tally_expense` had no currency column while `tally_obligation` and `core_transaction` carried one. | **fixed** — currency on `tally_group`, `tally_expense` and `tally_settlement`, held to the group by trigger (ruling R1) |
| **ONT-14** | Cross-file pointers (`core_link.provenance_id`, `share_authority.receipt_id` → `journal.db`) were gateway-enforced and unswept. | **fixed** by removing the seam: there is one file. Where the pointer is still deliberately a value rather than a key, the audit outlives its subject. |
| **ONT-15** | Twenty physical tables outside the registry, each local or rebuildable by design, but the reason was a per-file comment. | **fixed** — `localTables` is one list, and `commitments.rs` fails when a physical table is neither registered nor declared local or private. Four commons control tables that had been shelved as "device observation" turned out to be control truth and became registered entities; [#929](https://github.com/srikanth235/centraid/issues/929) then deleted the rail they belonged to, and the registry lost them with it. |
| **ONT-16** | The app grant plane — `access_grant`, `access_grant_scope`, `access_scope_tombstone`, `access_scope_request` and `access_seed_row`'s dependence on grants — grants authority to a principal that does not exist: a first-party app is the owner's own screen. #916 left it standing as [#883](https://github.com/srikanth235/centraid/issues/883) V-split's "app-strategy machinery"; that is a citation, not a property. | **closed by [#928](https://github.com/srikanth235/centraid/issues/928)** ([#963](https://github.com/srikanth235/centraid/pull/963) w2/w3, [#983](https://github.com/srikanth235/centraid/pull/983) w4/w5c). A first-party app holds no grant, and `evaluate_access` (`crates/vault/src/access.rs`) has no app path: an owner device reads its owner's vault directly, the assistant rides the acting owner, and everything else needs a `share_authority` row. `access_grant`, `access_grant_scope`, `access_policy`, `access_scope_tombstone` and `access_scope_request` exist in no DDL; `access_app` survives as the install register. |
| **ONT-17** | Purposes and the DPV vocabulary: 8 apps and 29 automations declare exactly one purpose (`dpv:ServiceProvision`), and `access_receipt.purpose_concept_id` holds a purpose **string** in a column typed as a concept id. The vocabulary was kept "for the day sharing brings a second party"; that day came in [#825](https://github.com/srikanth235/centraid/issues/825) and sharing did not use it. | **closed by [#928](https://github.com/srikanth235/centraid/issues/928)** ([#983](https://github.com/srikanth235/centraid/pull/983) w4, close follow-up). `access_receipt` has no `purpose_concept_id` — the column is gone with the whole purpose vocabulary from the receipt shape — and no manifest under `crates/apps/` declares a purpose. |
| **ONT-18** | `access_policy` has **zero** non-test writers and has been seeded empty since ONT-06, yet `evaluateAccess` consults it twice per non-owner read. ONT-06 kept three of its four kinds (`retention` / `purpose` / `minimization`) rather than asking who writes them. | **closed by [#928](https://github.com/srikanth235/centraid/issues/928)** ([#983](https://github.com/srikanth235/centraid/pull/983) w4). The table and both consultations are gone with the evaluator's app path: `crates/vault/src/access.rs` names no policy at all, and an owner-device read of the owner's own vault runs zero grant statements. |
| **ONT-19** | One receipt column, four id spaces: `access_receipt.grant_id` is written from `access_grant`, `share_authority`, `share_circle_grant` and `outbox_grant`, so the "last used" the Access dashboard promises is uncomputable and no foreign key can hold the pointer. | **closed by [#928](https://github.com/srikanth235/centraid/issues/928)** ([#983](https://github.com/srikanth235/centraid/pull/983) w4c). `access_receipt.authority_id` is the one id space — NULL means owner-direct, the one act that needs no answer — indexed as `idx_receipt_authority(authority_id, occurred_at)` so "last used" is one keyed lookup per row rather than a scan of the band. |
| **ONT-20** | `devices.grant_profile_json` is a real attenuation (a companion device confined to a set of surfaces) stored as gateway-side JSON on an enrollment, so the answer is invisible to the authority plane, to the dashboard and to the purge cascade — the shape [ONT-rows](decisions.md#ontology-v0-close-916) forbids. The same is true of `outbox_grant`. | **closed by [#928](https://github.com/srikanth235/centraid/issues/928) wave 5a.** Companion attenuation is `share_authority` rows (principal `device`, subject type `app.surface`), and `outbox_grant` is gone: the standing egress answer is a row with `subject_type = 'egress'`, principal `automation` or `device`, referenced by `outbox_item.authority_id`. `grant_profile_json` has no reader. |
| **ONT-21** | The give-plane residue: `share_edges`, `share_effects`, `share_access_receipts`, the coordinator, the edge store, the edge routes, the peer-plane sweep's edge half and the retire pass outlived the verb [#825](https://github.com/srikanth235/centraid/issues/825) retired. What they serve now is same-owner placement, which is a command. | **closed by [#928](https://github.com/srikanth235/centraid/issues/928) wave 5b.** `share_edges`, `share_effects` and `share_access_receipts` exist in no DDL, and there is no give plane. |
| **ONT-22** | _Reader-side drift._ Document history is a content→content `core_link` graph with no document identity, kept beside `core_entity_revision`, which [#916](https://github.com/srikanth235/centraid/issues/916) ruled "the only history table": `recordRevision` writes the second graph and restore walks it. Two documents with identical bytes therefore share one history, A→B→A→B cannot be expressed, and another object's revision can be restored. | **fixed** by rung two. [#996](https://github.com/srikanth235/centraid/issues/996) wave 0b made a revision an occurrence — an immutable per-document / per-note revision row pointing to content and, where needed, a parent revision — and deleted the content→content edges ([R20(a)](decisions.md#one-vault-every-seat-996)); that left the chain reader-enforced, because the schema still permitted a self-parent, a parent belonging to another object, an `UPDATE` re-pointing `parent_revision_id` (the only way a cycle can be closed) and a `revises` `core_link`. [`contracts/migrations/002_revisions.sql`](../contracts/migrations/002_revisions.sql) makes all four unwritable and refuses to run over a file that already carries one (`crates/vault/tests/revisions_migration.rs`, D-1020-N2); [`crates/apps/notes`](../crates/apps/notes/src/version_chain.rs) still answers a malformed chain with a typed refusal. |
| **ONT-23** | _Reader-side drift._ Tally's `pairwise` accumulates minor units into a map keyed by **party alone**, and the dashboard labels the sum with the vault's base currency — so a EUR 100 debt and a USD 100 debt read as one 200. [#916](https://github.com/srikanth235/centraid/issues/916)'s [ONT-currency](decisions.md#ontology-v0-close-916) put the currency on `tally_group`, `tally_expense` and `tally_settlement`; the aggregate drops it. | **closed by [#996](https://github.com/srikanth235/centraid/issues/996) wave 0d.** `Money` (`crates/apps/kit/src/money.rs`) is minor units **plus** an ISO 4217 code, and adding across currencies is an error rather than a third number. `pairwise` answers a `MoneyBag` keyed `(party, currency)`; a group's figures are in the group's own currency; the two hero totals are `Valuation`s that read `unavailable` with their components when the position spans currencies and no rate source exists — there is no rate plane in the product, and inventing one was the defect. The Tally crate's balance folds (`crates/apps/tally/src/balance.rs`) carry `Money`, so a bare number is unrenderable as a balance. See [R22](decisions.md#one-vault-every-seat-996). |
| **ONT-24** | `core_transaction.external_id` is globally `UNIQUE` while the comment five lines above says identity is `(connection_id, external_id)`; the publisher probes the global column when the sync map misses, and `accountFor` matches by owner plus display name. Two institutions' `ref-1` are one row, and two accounts sharing a display label are one account. | **closed by [#996](https://github.com/srikanth235/centraid/issues/996) wave 0b (schema) and 0c (the probe).** The sync map's `(connection_id, external_id)` is authoritative, the global `UNIQUE` and the global probe are gone, and an account is identified by `core_account.external_ref` — a SOURCE-SCOPED identifier the importer states — never by the display label. v1 ships no statement importer, so the schema half is what holds today. Two banks' "Savings" statements stay two accounts; merging them is a cross-source MATCH the owner accepts (**OQ-12**) and is deliberately not inferred. See [R20(c)](decisions.md#one-vault-every-seat-996). |
| **ONT-25** | _Reader-side drift._ The recurrence exception is written as `original_start_local`, and the Agenda web reader, the native reader and Tally's template dashboard read `original_start` — with `time_zone` where the schema says `tz`. [#916](https://github.com/srikanth235/centraid/issues/916)'s [ONT-recur](decisions.md#ontology-v0-close-916) ruled recurrence honest about time on storage; a skipped occurrence still shows on the phone. | **closed by [#996](https://github.com/srikanth235/centraid/issues/996) wave 0b (the column set) and 0c (the adapter).** `OccurrenceKey` — series identity plus the series-local wall clock plus semantics — is one typed value in `crates/vault/src/time/occurrence.rs`, and exceptions match on that wall clock rather than on the resolved instant (which is why a zoned series' skips matched nothing at all). See [R21](decisions.md#one-vault-every-seat-996). |
| **ONT-26** | Atlas's shared pre/postconditions are empty. Its guards are structural (unknown columns, PK edits, sealed columns, append-only tables) and its semantic set is not there at all: it accepts a task as its own parent, a content hash of 64 zeroes over unchanged bytes, and February 31 where `people.add_important_date` refuses it. Domain commands, imports and Atlas each enforce a different subset of the model. | **closed by [#996](https://github.com/srikanth235/centraid/issues/996) wave 0c.** There is no generic row writer in the current tree, and simple invariants are CHECKs and triggers, so they hold for every writer by construction: `schedule_task.due_at` must read as a time, `parent_task_id <> task_id`, `schedule_task_hierarchy_is_acyclic`, `schedule_task_section_agrees_with_project`, `people_important_date.month_day` must be a real day, `core_content_item.content_hash` must be sixty-four hex characters and `core_content_item_hash_follows_bytes`. See [R21](decisions.md#one-vault-every-seat-996). |
| **ONT-27** | People and Tasks complete the same `schedule_task` row with **different SQL**, and Tasks' recurrence copies columns into a fresh row with no series identity and without the `about` link — so the successor of a recurring "call Mum" is no longer about Mum, and which app completed a task decides what happens. | **closed by [#996](https://github.com/srikanth235/centraid/issues/996) wave 0c.** `crates/vault/src/operations/task_lifecycle.rs` is the one completion: `complete` / `reopen` / `cancel`, each idempotent and none a toggle. `people.toggle_task` is **deleted** — People calls `people.complete_task` / `people.reopen_task`, Tasks calls `schedule.set_task_status`, and both land here. `schedule_task.series_id` is the stable series identity (the head carries its own id, every occurrence carries the head's) and the successor re-asserts the completed occurrence's live `core_link` rows, so the successor of a recurring "call Mum" is still about Mum. See [R21](decisions.md#one-vault-every-seat-996). |
| **ONT-28** | `media_type` lives on the hash-deduped content row, so the first import fixes the interpretation of identical bytes for every later document: the same bytes filed once as `text/html` are `text/html` forever, for every owner of that sha. An interpretation is being stored as a property of bytes. | **closed** by [#996](https://github.com/srikanth235/centraid/issues/996) wave 0b. `core_content_item` is bytes alone (uri, sha, size, language) and `core_content_representation` carries `media_type`, `charset` and `interpretation`, one row per `(owner_type, owner_id)`; byte dedupe is untouched. The authored title is the wrapper's — `media_asset.title` joins `core_document.title` and `knowledge_note.title` — and a generated caption is a `knowledge.annotation` on the representation, promoted to a title only by `media.promote_caption` (**OQ-9**). The read side's one spelling is `crates/apps/kit/src/representations.rs`. See [R20(b)](decisions.md#one-vault-every-seat-996). |
| **ONT-29** | `tagNotation` lowercases and strips `[^a-z0-9]`, so `猫`, `犬`, `कुत्ता` and `बिल्ली` all normalise to `untitled` and select **one** concept. A label is being used as identity, and the collapse is silent. | **closed for the collapse; the key column has no writer.** [#996](https://github.com/srikanth235/centraid/issues/996) wave 0b added `core_concept.normalized_key` with a partial unique index on `(scheme_id, normalized_key)` ([R20(d)](decisions.md#one-vault-every-seat-996)). The current writer, `core.tag_item` in `crates/vault/src/commands/core.rs`, selects and inserts on `notation` = the label trimmed, lowercased and whitespace-collapsed (`notation_of`), which preserves 猫 and 犬 as two concepts, and writes no `normalized_key` at all — so the column the ruling added is NULL on every concept this tree mints, and the notation is not NFKC-normalised. |
| **ONT-30** | The identifier-**value** index is partial on `valid_to IS NULL`; the primary-**preference** index is not. An end-dated primary therefore blocks a new one: the current preference is held to a uniqueness rule that spans history. | **closed by [#996](https://github.com/srikanth235/centraid/issues/996) wave 0b.** `idx_party_identifier_primary` is partial on `is_primary = 1 AND valid_to IS NULL`, like the value index; interval ordering is a table CHECK; and `issuer` is the namespace a short handle is unique within. See [R20(e)](decisions.md#one-vault-every-seat-996). |
| **ONT-31** | `schedule.add_task` accepts `due_at: "banana"` and `rrule: "garbage"`. The row is stored, and completion then silently has no successor — an unparseable rule is indistinguishable from no rule at read time. | **closed by [#996](https://github.com/srikanth235/centraid/issues/996) wave 0c.** `crates/vault/src/time/temporal.rs` names the four readings once — instant, floating local datetime, local date, yearless month-day — and checks the calendar, not just the shape. A COMMAND refuses `due_at: "banana"`, `2026-02-31` and `rrule: "garbage"` with the operation's sentence; an IMPORT keeps the provider's rule and marks it `core_event.rrule_support = 'unsupported'`, because expanding to nothing reads exactly like "this does not repeat". Refusals are also structural: `schedule_task.due_at` carries the CHECK. See [R21](decisions.md#one-vault-every-seat-996). |
| **ONT-32** | Derived data written onto a member-edited row moves that row's `row_version`, and the gateway's conflict check is row-level: `media.set_place_gazetteer` owns only `$.gazetteer` inside `core_place.address_json` and never touches `name`, but a background match refuses the member's offline rename of that place against a change they cannot see and cannot reconcile ([#1014](https://github.com/srikanth235/centraid/issues/1014) B11). | **open.** A column-level exemption in the touch trigger cannot express it — the derived data is a sub-document inside a column that also holds the member's own address — so the fix is the one [#1014](https://github.com/srikanth235/centraid/issues/1014)'s R-1014-5 states: derived data is not replicated data, and `$.gazetteer` belongs in a derived row keyed to the place. That is a ladder rung and an ontology ruling, not a trigger clause. The write site is `media.set_place_gazetteer` in `crates/vault/src/commands/media.rs`. |

## Was the starting design right?

The spine was. Every domain that shipped anchored into it without forking identity: People is a profile on `core.party`, Tally is a decoration on `social.circle` with settlements that post to `core.transaction`, Docs is `core.document` over `core.content_item` filed by `core.tag`, Photos is `core.content_item` with capture context, and the one exemption (Locker) is written down. The universal joins (`tag`, `link`, `annotation`, `collection`, `attachment`) absorbed stars, memos, employment, albums, notebooks, receipts and folders, which is what they were for. The rules caught real drift four times — [#274](https://github.com/srikanth235/centraid/issues/274), [#441](https://github.com/srikanth235/centraid/issues/441), [#883](https://github.com/srikanth235/centraid/issues/883), [#916](https://github.com/srikanth235/centraid/issues/916) — and each time the fix was a mechanism, not a patch.

Four things were started wrong, and the register above is their residue:

1. **Breadth before consumers.** The foundation release modelled eight life domains at once; the page's own first-boundary advice ("do not add a third domain until export→reimport round-trips") was better than the plan that ignored it. Four domains and five mechanisms have now left for the same reason: no producer, no consumer.
2. **A consent plane borrowed from a different product.** App grants, registered views, purpose policy and export jobs were modelled Solid/ODRL-style before Centraid's real authority model existed. That model turned out to be owner + device + one authority table ([#726](https://github.com/srikanth235/centraid/issues/726), [#883](https://github.com/srikanth235/centraid/issues/883)). What survives of it is `access_app` — the install register (name, label, its signing key, status) — and the audit band's `access_receipt`. The grant tables, the policy table and the evaluator's app path are gone: a first-party app is the owner's own screen, its reach is fixed at build time by its declared entity manifest and the static tripwire over it, and an owner-device read of the owner's own vault runs zero grant statements ([#928](https://github.com/srikanth235/centraid/issues/928) AP-owner-direct).
3. **Conventions where declarations belonged.** A per-row version stamp, a soft-delete pair "adopted as tables are touched", a polymorphic pointer registry swept by hand — each was a convention, and a convention without a mechanism drifts by construction. The registry is where such things are declared, and the engine is where they are enforced: the supertype turned pointer hygiene from a sweep into a foreign key, and the lifecycle declaration turned timestamps from a habit into a test.
4. **A second file for a size problem.** `journal.db` traded atomicity and referential integrity for a smaller sovereign asset. Retention was the answer all along; the split was a cost paid every transaction for a benefit that arrived once a year.

What was missing was never a better model but a guard: nothing compared the design with the schema, so the design aged silently. The commitment tests close that for the conventions and `sql-confinement` closes it for the door; the page is open again (ONT-01).

## Adding or changing a table

The admission checklist on the page (§11, [#310](https://github.com/srikanth235/centraid/issues/310) C6) still governs. In this repo that means, in the same PR:

- a new rung under `contracts/migrations/`, appended to `LADDER` in `crates/vault/src/migrations.rs` — a released rung is never edited — with the entity's row in `core_entity` following from its membership trigger;
- the registry entry in `contracts/schema/v0-registries.json` with its label, **lifecycle** and deletion roles — or `projectionOf` if the row is a part of an entity — or an entry in `localTables` / `privateTables`;
- a composite foreign key into `core_entity` if the table carries a `(type, id)` pair;
- the `contentReferences` entry if it rents bytes; an FTS index if it has text (with a deleted-row filter if it trashes);
- the typed commands in `crates/vault/src/commands` and the app manifest's scopes;
- the §03 entry on the published page;
- and this document, if a commitment or a register row changes.

## How this model is reviewed

A model review that only reads is not a review. [#916](https://github.com/srikanth235/centraid/issues/916) established three practices, now part of the repo's conventions (see [AGENTS.md](../AGENTS.md)):

- **A citation is not a justification.** Every "deliberate", "by design" or `#NNN`-ruled seam is re-judged on its merits for the product as it is now. A ruling explains why something was chosen, never whether it should stay. An item worth keeping gets an explicit question to the owner with options and a recommendation; an item that survives only because someone ruled it, with no consumer or security property depending on it, is a finding. This wave filed polymorphic references, the favorite mirror and the consent plane as settled on exactly that deference, and had to reopen all three.
- **Mechanical sweeps are part of the review.** Every CHECK value against its writers; every FK's delete rule against its siblings; every table's timestamps against its lifecycle; every raw-SQL site outside the package. Reading found none of the dead CHECK values, none of the inconsistent delete rules, and none of the missing currency columns.
- **An adversarial run against the real gateway is part of the review.** Twelve scenarios driven through the shipping command pipeline reproduced the merge that deleted a party's expense splits, the purge sweep that wedged on one refused row, the export that shipped the data-encryption key in the clear, and the revoke a trashed projection could evade. None of those were visible by reading.

The corollary for a future wave: when a register row says **fixed**, the row names the mechanism, and the mechanism has a test in the commitments table above. A finding with no mechanism is not fixed; it is deferred, and it says so.
