# Your overlay for the `doc-integrity` directive.
#
# This file is YOURS. `governance pack update`, `reset`, and `kit update` never
# touch it — your edits are sacred. It starts empty: with nothing below, the
# directive runs on its author-owned defaults. Delete this file to do the same.
#
# The live defaults AND their documentation are in the pack-owned directive.yaml, refreshed on
# every `governance pack update`:
#
#   .governance/packs/governance-kit/audit/directives/doc-integrity/directive.yaml
#
# Syntax — your lines layer over those defaults:
#
#   KEY=value     override a scalar declared with `tunable: true`
#   KEY+=<item>   add an item to a named list
#   KEY-=<item>   drop one of a named list's pack-owned defaults
#   <item>/!item  shorthand only when the manifest declares one list key
#
# Blank lines and `#` comments are ignored. See the directive.yaml linked above
# for this directive's specific knobs, rule syntax, and examples. Entries with
# `tunable: false` are author-fixed semantics and ignore overlay rows.

# Restored after audit v0.10.0 → v0.11.0 (duaility/governance-kit#370), which
# slimmed the shipped RULES default to receipts + the Evolution Log. These three
# ledgers are append-only in this repo, so the freeze stays.
RULES+=frozen-files COSTS.md
RULES+=frozen-files STEERING.md
RULES+=frozen-section QUALITY.md Resolved
