# agent-steering-accounting — Detected human steering events recorded in STEERING.md, summarised by per-commit Steer-Count/Types/Tiers trailers.
category: AgentDiscipline
recommended: true
always_install: true
summary: Append-only ledger of human steering events (interrupts and classifier-confirmed corrections); per-commit summary trailers (Steer-Count/Types/Tiers) tally the rows added by each commit.
surface: change-set
hook: commit-msg
reads:
  - STEERING.md
writes:
  - STEERING.md
