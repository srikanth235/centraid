# agent-token-accounting — Each agent commit's token cost lands in the issue's receipt; a commit-time check reconciles the receipt's recorded cumulative against the transcript (no commit trailers).
category: AgentDiscipline
recommended: true
summary: Each agent commit's token cost lands in the issue's receipt (receipts/issue-<N>.md); a commit-time check reconciles the receipt's recorded cumulative against the transcript (no commit trailers).
surface: change-set
hook: commit-msg
