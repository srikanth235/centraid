# Intentionally ungated surfaces (#532 / TESTING.md). One package id per line
# (e.g. packages/test-kit). A row also covers that id's non-`src` executable
# trees. Comments start with #.
#
# The v0 `apps/*` rows (desktop, extension, web, and the mobile native-module,
# plugin, polyfill and iOS share-extension trees) left with the v0 TypeScript
# tree (#1020): their targets no longer exist under this directive's scope.
#
# Dev-only / test-support packages without a whole-tree floor:
packages/test-kit
