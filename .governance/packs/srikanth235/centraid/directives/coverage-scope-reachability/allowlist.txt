# Intentionally ungated surfaces (#532 / TESTING.md). One package or app id per
# line (e.g. packages/protocol, apps/desktop). Comments start with #.
# Journey / pure-module packages without a whole-tree floor:
packages/blob-format
packages/design-tokens
packages/protocol
packages/cli
packages/tunnel
packages/test-kit
# client: only packages/client/src/replica/** is floored; the rest is journeys.
packages/client
# Renderer / installable clients: e2e + extracted units, not line floors.
apps/desktop
apps/extension
apps/mobile
apps/web
