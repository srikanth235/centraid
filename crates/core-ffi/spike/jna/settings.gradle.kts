// The JVM half of the binding spike (#1020, D-1020-D2-7).
//
// A standalone Gradle build rather than a module of anything: the Android shell
// is wave 3 lane E's and does not exist yet, and a spike that had to be wired
// into a shell before it could run would not be a spike.
rootProject.name = "centraid-jna-spike"
