{
  "algorithm": "sha256-of-sorted-file-sha256",
  "schemaFingerprint": "30eed7f32cac8dca4a5564c58c82aa7e7d478d5d76e0067223fc28e356753d73",
  "exportOwner": "packages/vault/src/gateway/portable-export.ts",
  "approvedDeviation": "#996 (issue-996) W5, the old-store cut, re-pinned ONCE over the #929 baseline. What moved in packages/vault/src/schema:\n(1) `read-path-indexes.ts` is NEW and states thirty composite indexes over tables the canonical walk already carries. Every one of them removes a `USE TEMP B-TREE FOR ORDER BY` from `packages/server/src/serve/app-query-plans.snapshot.md`, which went 36 -> 0 in the same slice; the shape is `(equality predicate columns, sort column, primary key)`, measured rather than assumed. Stated in the baseline with the golden corpus re-frozen, not as a rung.\n(2) `replica.ts` bumps REPLICA_SCHEMA_EPOCH 2 -> 3 — the ONE bump for W5. It is the seat's compatibility number: a mismatch is a re-bootstrap, never a partial apply.\nEXPORT COMPLETENESS: nothing entered or left the walk. An index is derived from the baseline DDL a restore replays, and the schema epoch is minted fresh in the restored file; carrying either would put a second copy of a stated fact into a bundle. The audit is recorded at the owner, packages/vault/src/gateway/portable-export.ts."
}
