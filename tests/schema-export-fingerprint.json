{
  "algorithm": "sha256-of-sorted-file-sha256",
  "schemaFingerprint": "6f5a1c1145ed3924b3baf941c664d2c99600859c5ca5db068132fc4d2703a854",
  "exportOwner": "packages/vault/src/gateway/portable-export.ts",
  "approvedDeviation": "#872 Tally backend: schema fingerprint moved for two new registered tables (tally_expense_payer, tally_nudge) and four new columns (tally_expense.split_method / split_params_json, tally_group.simplify_opt_in / archived_at), plus a nullable tally_expense.group_id and a tally_expense_line_item that now hangs off the expense with a nullable receipt_id. Export completeness was re-audited in packages/vault/src/gateway/portable-export.ts: both tables are registered in schema/tables.ts so they enter the canonical walk, and exportVault's SELECT * carries every new column with no adapter. All six are ground facts or owner decisions — a restore that dropped them would return expenses nobody paid for, a lost prepared reminder, edits that re-open as exact amounts, a silently re-wired simplification opt-in, or an archived group back in the lists. #872 Locker backend: the fingerprint moved again for five newly registered tables (locker_item_alias, which existed in DDL but was outside the canonical walk, plus locker_item_field, locker_item_address, locker_item_passkey and locker_item_history) and three locker_item changes (password_set_at, archived_at, and a type CHECK widened from six to fifteen item types). Export completeness was re-audited in the same owner file: all five are registered in schema/tables.ts so they enter the walk, and exportVault's SELECT * carries the new columns with no adapter. Every one is a fact the owner entered or a record only this vault holds — an unregistered alias table silently broke every connector binding on restore, and history is the only record that a password was ever rotated. The sidecars' sealed cells (locker_item_field.value_sealed, locker_item_passkey.private_key, locker_item_history.password) stay ciphertext in the artifact exactly like locker_item.password. #873 integration: the fingerprint moved once more for a change inside packages/vault/src/schema/sealed.ts that adds NO table and NO column — the three Locker sidecar sealed columns joined SEALED_PAYLOAD_FIELDS alongside SEALED_COLUMNS, closing a draft-band leak the extended T3 canary caught (a staged import row would have carried a custom field's sealed value, a previous password and a passkey's private key in the clear). Export completeness is unchanged by it and was re-checked anyway: SEALED_PAYLOAD_FIELDS governs sync_import_row payload staging, not the canonical walk, and every one of those cells still leaves exportVault as ciphertext exactly like locker_item.password."
}
