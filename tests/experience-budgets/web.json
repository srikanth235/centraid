{
  "_comment": "Experience budgets for the installable PWA (apps/web). Perceived latency only — byte and request ceilings live in apps/web/tests/e2e/perf-budgets.ts. See README.md in this directory for the status vocabulary and the year-3 volume table. Ratcheted tighten-only via PERF_BUDGET_SOURCES.",
  "surface": "web",
  "metrics": {
    "coldOpenToUsable": {
      "status": "unmeasured",
      "volume": "empty (web-e2e fixture vault)",
      "probe": "apps/web/tests/e2e/perf-waterfall.spec.ts — 'web vitals — LCP / INP / CLS on a cold shell load'",
      "_note": "Blocked on the same missing first-contentful-paint as largestContentfulPaint. What IS observable in this harness today (2026-07-31 run): domContentLoadedEventEnd 32-52 ms, loadEventEnd 48-96 ms, first-paint 44 ms. Those are document-lifecycle events, not 'first usable screen', so they are recorded here as context and NOT promoted to a ceiling. perfBudgets.timing.coldOpenMsSoftCeiling (15,000 ms, enforceTiming === true) is the machine-cost twin that gates today."
    },
    "largestContentfulPaint": {
      "status": "unmeasured",
      "volume": "empty (web-e2e fixture vault)",
      "probe": "apps/web/tests/e2e/perf-waterfall.spec.ts — 'web vitals (LCP/INP/CLS) on a cold shell load'",
      "_intendedCeilingMs": 2500,
      "_provenance": {
        "attempted": "2026-07-31, darwin arm64, Playwright bundled headless Chromium, apps/web e2e harness",
        "result": "NO LCP ENTRY. The probe ran, all three observers installed (`installed: [largest-contentful-paint, layout-shift, event]`), the connect screen rendered fully (accessibility snapshot shows heading + ticket field), and the paint timeline contained ONLY `first-paint=44` — no `first-contentful-paint`, hence no LCP candidate, after 16 s.",
        "note": "The ceiling is deliberately parked in `_intendedCeilingMs` (underscore = invisible to the ratchet) rather than shipped as `ceilingMs`. Shipping the Core Web Vitals 2500 ms threshold as a live ceiling would gate on a value the browser never emits, which passes vacuously forever. First suspect for the missing FCP: shell content that is not yet a contentful paint to the compositor (webfont with font-display: block, or an opacity/transition that never resolves in this harness). Promote to `ceilingMs` the moment a run produces a number."
      }
    },
    "interactionToNextPaint": {
      "status": "unmeasured",
      "volume": "empty (web-e2e fixture vault)",
      "probe": "apps/web/tests/e2e/perf-waterfall.spec.ts — 'web vitals (LCP/INP/CLS) on a cold shell load'",
      "_intendedCeilingMs": 200,
      "_provenance": {
        "attempted": "2026-07-31, same run as largestContentfulPaint",
        "result": "NO EVENT-TIMING ENTRY. The cold connect screen exposes no visible button, so the probe's deliberate click timed out (`interactionDriven: false`) and Chromium recorded no interaction. INP is undefined without one.",
        "note": "Parked in `_intendedCeilingMs` for the same reason as LCP. Closing this needs the probe to interact with a control that exists on the cold screen — the pairing-ticket textbox is present; a keypress-driven INP capture is the obvious next step and does not need any apps/web/src change."
      }
    },
    "cumulativeLayoutShift": {
      "status": "measured",
      "volume": "empty (web-e2e fixture vault)",
      "probe": "apps/web/tests/e2e/perf-waterfall.spec.ts — 'web vitals (LCP/INP/CLS) on a cold shell load'",
      "maxScore": 0.1,
      "_provenance": {
        "at": "2026-07-31",
        "host": "darwin arm64, Playwright bundled headless Chromium, apps/web e2e harness",
        "observedScore": 0,
        "note": "MEASURED and HARD-GATED — the one vital this harness produces honestly, and the only one of the three currently asserted. Observed 0 (no unexpected layout shift on the cold connect screen). The ceiling is the Core Web Vitals 'good' threshold rather than observed+headroom, because a 0 + headroom ceiling would be 0 and any first honest shift would red the lane; 0.1 is the point at which a user notices.",
        "headroom": "standard-derived (0.1 threshold vs observed 0)"
      }
    },
    "tapToVisualResponse": {
      "status": "unmeasured",
      "volume": "empty (web-e2e fixture vault)",
      "probe": "apps/web/tests/e2e/perf-waterfall.spec.ts — the INP capture above is the closest proxy; a dedicated per-control probe does not exist"
    },
    "sendToFirstToken": {
      "status": "unmeasured",
      "volume": "n/a — the web e2e harness has no model provider",
      "probe": "NONE TODAY. Needs a deterministic stub runner in apps/web/tests/e2e/server.ts that emits a first token on a fixed delay, so the measurement is the client's dead time and not the provider's."
    },
    "appWeight": {
      "status": "measured",
      "volume": "n/a — build artifact, independent of vault size",
      "probe": "scripts/perf/app-weight.mjs --surface web (weighs apps/web/dist produced by `bun run web:build`), run in CI after the web build",
      "maxTotalBytes": 11010048,
      "maxLargestChunkBytes": 2200000,
      "_provenance": {
        "at": "2026-08-15",
        "host": "linux x64 worktree, `bun run web:build` (vite build + precompress)",
        "observedShippedBytes": 10211472,
        "observedShippedFiles": 72,
        "observedLargestChunkBytes": 1995618,
        "observedLargestChunk": "centraid_web_iroh_bg-*.wasm",
        "note": "MEASURED by `node scripts/perf/app-weight.mjs --surface web --report` (#781, closing the #587 D21 web gap). The earlier 'a weigh-in here would be a duplicate owner' claim was wrong: perf-budgets.ts shell.maxTransferBytes fences what the BROWSER transfers on the e2e shell journey (compressed, journey-scoped), not what `web:build` ships on disk — the two can diverge by megabytes without either gate noticing the other's regression. Source maps plus the .br/.gz precompress sidecars (19.7 MB together) are excluded as diagnostics/alternate encodings of already-weighed bytes. The Iroh WASM binding ships TWICE (assets/ copy + centraid-worker-iroh.wasm worker copy, 2.0 MB each) — de-duplicating it is the single biggest cut available, and when it lands these ceilings must be re-measured and TIGHTENED. Next largest: pdf.worker.min.mjs 1.30 MB, boot.js 1.24 MB, sqlite3.wasm 865 kB.",
        "headroom": "observed + ~8% total; largest chunk + ~10% (mirrors the desktop/mobile seeds)"
      }
    }
  }
}
