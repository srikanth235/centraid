<!-- Generated by app-query-plans.test.ts (#996 R8). Review the diff; do not hand-edit. -->

# App query plans

## agenda

### agenda.upcoming.window (agenda/upcoming)

```sql
SELECT event_id, ical_uid, summary, description, dtstart, dtend, start_tz, end_tz, recurrence_semantics, rrule, rrule_support, status, location_place_id, organizer_party_id, sequence, created_at, updated_at
      FROM core_event
      WHERE status <> ? AND dtstart >= ? AND dtstart < ?
      ORDER BY dtstart ASC, event_id ASC
      LIMIT ?
```

```
SEARCH core_event USING INDEX core_event_dtstart_idx (dtstart>? AND dtstart<?)
USE TEMP B-TREE FOR LAST TERM OF ORDER BY
```

### agenda.upcoming.recurringAnchors (agenda/upcoming)

```sql
SELECT event_id, ical_uid, summary, description, dtstart, dtend, start_tz, end_tz, recurrence_semantics, rrule, rrule_support, status, location_place_id, organizer_party_id, sequence, created_at, updated_at
      FROM core_event
      WHERE status <> ? AND rrule IS NOT NULL
      ORDER BY dtstart DESC, event_id DESC
      LIMIT ?
```

```
SCAN core_event USING INDEX core_event_dtstart_idx
USE TEMP B-TREE FOR LAST TERM OF ORDER BY
```

### agenda.upcoming.calendars (agenda/upcoming)

```sql
SELECT calendar_id, owner_party_id, name, color, default_tz, visibility
      FROM schedule_calendar

      ORDER BY calendar_id ASC, calendar_id ASC
      LIMIT ?
```

```
SCAN schedule_calendar USING INDEX sqlite_autoindex_schedule_calendar_1
```

### agenda.dayContext.birthdays (agenda/day-context)

```sql
SELECT party_id, display_name, kind, birth_date
      FROM core_party
      WHERE kind = ? AND birth_date IS NOT NULL
      ORDER BY party_id ASC, party_id ASC
      LIMIT ?
```

```
SCAN core_party USING INDEX sqlite_autoindex_core_party_1
```

### agenda.dayContext.dueTasks (agenda/day-context)

```sql
SELECT task_id, status, title, due_at, project_id
      FROM schedule_task
      WHERE status IN (?, ?) AND due_at >= ? AND due_at < ?
      ORDER BY due_at ASC, task_id ASC
      LIMIT ?
```

```
SEARCH schedule_task USING INDEX schedule_task_due_at_idx (due_at>? AND due_at<?)
USE TEMP B-TREE FOR LAST TERM OF ORDER BY
```

### agenda.dayContext.flagsScheme (agenda/day-context)

```sql
SELECT scheme_id, uri
      FROM core_concept_scheme
      WHERE uri = ?
      ORDER BY scheme_id ASC, scheme_id ASC
      LIMIT ?
```

```
SEARCH core_concept_scheme USING INDEX sqlite_autoindex_core_concept_scheme_2 (uri=?)
```

### agenda.parties.vault (agenda/parties)

```sql
SELECT vault_id, self_party_id
      FROM core_vault

      ORDER BY vault_id ASC, vault_id ASC
      LIMIT ?
```

```
SCAN core_vault USING INDEX sqlite_autoindex_core_vault_1
```

### agenda.parties.people (agenda/parties)

```sql
SELECT party_id, display_name, kind
      FROM core_party
      WHERE kind = ?
      ORDER BY party_id ASC, party_id ASC
      LIMIT ?
```

```
SCAN core_party USING INDEX sqlite_autoindex_core_party_1
```

## docs

### _shared/taxonomy.concepts (docs/drive)

```sql
SELECT concept_id, scheme_id, pref_label, notation
      FROM core_concept

      ORDER BY concept_id ASC, concept_id ASC
      LIMIT ?
```

```
SCAN core_concept USING INDEX sqlite_autoindex_core_concept_1
```

### _shared/taxonomy.schemes (docs/drive)

```sql
SELECT scheme_id, uri, title
      FROM core_concept_scheme

      ORDER BY scheme_id ASC, scheme_id ASC
      LIMIT ?
```

```
SCAN core_concept_scheme USING INDEX sqlite_autoindex_core_concept_scheme_1
```

### docs.origins.subscriptions (docs/drive)

```sql
SELECT authority_id, origin_vault_id, subscribed_at
      FROM share_subscription
      WHERE state = ?
      ORDER BY subscribed_at DESC, authority_id DESC
      LIMIT ?
```

```
SCAN share_subscription
USE TEMP B-TREE FOR ORDER BY
```

### docs.history.document (docs/history)

```sql
SELECT document_id, current_content_id, current_revision_id, created_at
      FROM core_document
      WHERE document_id = ?
      ORDER BY document_id ASC, document_id ASC
      LIMIT ?
```

```
SEARCH core_document USING INDEX sqlite_autoindex_core_document_1 (document_id=?)
```

### docs.activity.provenance (docs/activity)

```sql
SELECT prov_id, prov_activity, agent_kind, occurred_at
      FROM access_provenance
      WHERE entity_type = ? AND entity_id = ?
      ORDER BY occurred_at DESC, prov_id DESC
      LIMIT ?
```

```
SEARCH access_provenance USING INDEX idx_provenance_entity (entity_type=? AND entity_id=?)
USE TEMP B-TREE FOR ORDER BY
```

## locker

### locker.autofill.logins (locker/autofill-candidates)

```sql
SELECT item_id, type, title, username, url, url_match_policy, notes, cardholder, expiry, brand, fullname, email, phone, address, network, connection_id, compromised, password_set_at, created_at, updated_at, archived_at, deleted_at, purge_at
      FROM locker_item
      WHERE type = ? AND deleted_at IS NULL
      ORDER BY updated_at DESC, item_id DESC
      LIMIT ?
```

```
SEARCH locker_item USING INDEX locker_item_type_idx (type=?)
USE TEMP B-TREE FOR ORDER BY
```

### locker.autofill.item (locker/autofill-item)

```sql
SELECT item_id, type, title, username, url, url_match_policy, notes, cardholder, expiry, brand, fullname, email, phone, address, network, connection_id, compromised, password_set_at, created_at, updated_at, archived_at, deleted_at, purge_at
      FROM locker_item
      WHERE item_id = ? AND type = ? AND deleted_at IS NULL
      ORDER BY item_id ASC, item_id ASC
      LIMIT ?
```

```
SEARCH locker_item USING INDEX sqlite_autoindex_locker_item_1 (item_id=?)
```

### locker.items.archived (locker/items)

```sql
SELECT item_id, type, title, username, url, url_match_policy, notes, cardholder, expiry, brand, fullname, email, phone, address, network, connection_id, compromised, password_set_at, created_at, updated_at, archived_at, deleted_at, purge_at
      FROM locker_item
      WHERE deleted_at IS NULL AND archived_at IS NOT NULL
      ORDER BY updated_at DESC, item_id DESC
      LIMIT ?
```

```
SCAN locker_item
USE TEMP B-TREE FOR ORDER BY
```

### locker.items.concepts (locker/items)

```sql
SELECT concept_id, scheme_id, pref_label, notation
      FROM core_concept

      ORDER BY concept_id ASC, concept_id ASC
      LIMIT ?
```

```
SCAN core_concept USING INDEX sqlite_autoindex_core_concept_1
```

### locker.items.schemes (locker/items)

```sql
SELECT scheme_id, uri
      FROM core_concept_scheme

      ORDER BY scheme_id ASC, scheme_id ASC
      LIMIT ?
```

```
SCAN core_concept_scheme USING INDEX sqlite_autoindex_core_concept_scheme_1
```

### locker.item.row (locker/item)

```sql
SELECT item_id, type, title, username, url, url_match_policy, notes, cardholder, expiry, brand, fullname, email, phone, address, network, connection_id, compromised, password_set_at, created_at, updated_at, archived_at, deleted_at, purge_at
      FROM locker_item
      WHERE item_id = ?
      ORDER BY item_id ASC, item_id ASC
      LIMIT ?
```

```
SEARCH locker_item USING INDEX sqlite_autoindex_locker_item_1 (item_id=?)
```

### locker.search.items (locker/search)

```sql
SELECT item_id, type, title, username, url, url_match_policy, notes, cardholder, expiry, brand, fullname, email, phone, address, network, connection_id, compromised, password_set_at, created_at, updated_at, archived_at, deleted_at, purge_at
      FROM locker_item
      WHERE deleted_at IS NULL
      ORDER BY updated_at DESC, item_id DESC
      LIMIT ?
```

```
SCAN locker_item
USE TEMP B-TREE FOR ORDER BY
```

### locker.trash.items (locker/trash)

```sql
SELECT item_id, type, title, username, url, url_match_policy, notes, cardholder, expiry, brand, fullname, email, phone, address, network, connection_id, compromised, password_set_at, created_at, updated_at, archived_at, deleted_at, purge_at
      FROM locker_item
      WHERE deleted_at IS NOT NULL
      ORDER BY updated_at DESC, item_id DESC
      LIMIT ?
```

```
SCAN locker_item
USE TEMP B-TREE FOR ORDER BY
```

### locker.access.receipts (locker/access)

```sql
SELECT receipt_id, action, object_type, object_id, decision, occurred_at, detail_json
      FROM access_receipt
      WHERE object_type IN (?, ?) AND object_id = ?
      ORDER BY occurred_at DESC, receipt_id DESC
      LIMIT ?
```

```
SCAN access_receipt
USE TEMP B-TREE FOR ORDER BY
```

## notes

### _shared/journal.scheme (notes/link-targets)

```sql
SELECT scheme_id, uri
      FROM core_concept_scheme
      WHERE uri = ?
      ORDER BY scheme_id ASC, scheme_id ASC
      LIMIT ?
```

```
SEARCH core_concept_scheme USING INDEX sqlite_autoindex_core_concept_scheme_2 (uri=?)
```

### notes.library.recent (notes/library)

```sql
SELECT note_id, title, format, pinned, body_content_id, created_at, updated_at, deleted_at, purge_at
      FROM knowledge_note
      WHERE deleted_at IS NULL
      ORDER BY updated_at DESC, note_id DESC
      LIMIT ?
```

```
SCAN knowledge_note
USE TEMP B-TREE FOR ORDER BY
```

### notes.library.pinned (notes/library)

```sql
SELECT note_id, title, format, pinned, body_content_id, created_at, updated_at, deleted_at, purge_at
      FROM knowledge_note
      WHERE pinned = ? AND deleted_at IS NULL
      ORDER BY updated_at DESC, note_id DESC
      LIMIT ?
```

```
SCAN knowledge_note
USE TEMP B-TREE FOR ORDER BY
```

### notes.library.trash (notes/library)

```sql
SELECT note_id, title, format, pinned, body_content_id, created_at, updated_at, deleted_at, purge_at
      FROM knowledge_note
      WHERE deleted_at IS NOT NULL
      ORDER BY deleted_at DESC, note_id DESC
      LIMIT ?
```

```
SCAN knowledge_note
USE TEMP B-TREE FOR ORDER BY
```

### notes.library.notebooks (notes/library)

```sql
SELECT collection_id, name, sort_order
      FROM core_collection

      ORDER BY sort_order ASC, collection_id ASC
      LIMIT ?
```

```
SCAN core_collection
USE TEMP B-TREE FOR ORDER BY
```

### notes.note.row (notes/note)

```sql
SELECT note_id, body_content_id, format
      FROM knowledge_note
      WHERE note_id = ?
      ORDER BY note_id ASC, note_id ASC
      LIMIT ?
```

```
SEARCH knowledge_note USING INDEX sqlite_autoindex_knowledge_note_1 (note_id=?)
```

### notes.history.note (notes/history)

```sql
SELECT note_id, body_content_id, current_revision_id, created_at
      FROM knowledge_note
      WHERE note_id = ?
      ORDER BY note_id ASC, note_id ASC
      LIMIT ?
```

```
SEARCH knowledge_note USING INDEX sqlite_autoindex_knowledge_note_1 (note_id=?)
```

## people

### people.roster.profiles (people/people)

```sql
SELECT party_id, created_at, cadence_days, role, avatar_color, last_contacted_at, deleted_at
      FROM people_profile
      WHERE deleted_at IS NULL
      ORDER BY created_at DESC, party_id DESC
      LIMIT ?
```

```
SCAN people_profile
USE TEMP B-TREE FOR ORDER BY
```

### _shared/taxonomy.concepts (people/people)

```sql
SELECT concept_id, scheme_id, pref_label, notation
      FROM core_concept

      ORDER BY concept_id ASC, concept_id ASC
      LIMIT ?
```

```
SCAN core_concept USING INDEX sqlite_autoindex_core_concept_1
```

### _shared/taxonomy.schemes (people/people)

```sql
SELECT scheme_id, uri, title
      FROM core_concept_scheme

      ORDER BY scheme_id ASC, scheme_id ASC
      LIMIT ?
```

```
SCAN core_concept_scheme USING INDEX sqlite_autoindex_core_concept_scheme_1
```

### people.person.profile (people/person)

```sql
SELECT party_id, role, nickname, avatar_color, cadence_days, last_contacted_at, created_at, met, deleted_at
      FROM people_profile
      WHERE party_id = ? AND deleted_at IS NULL
      ORDER BY party_id ASC, party_id ASC
      LIMIT ?
```

```
SEARCH people_profile USING INDEX sqlite_autoindex_people_profile_2 (party_id=?)
```

### people.person.party (people/person)

```sql
SELECT party_id, display_name, kind
      FROM core_party
      WHERE party_id = ?
      ORDER BY party_id ASC, party_id ASC
      LIMIT ?
```

```
SEARCH core_party USING INDEX sqlite_autoindex_core_party_1 (party_id=?)
```

### people.dashboard.profiles (people/dashboard)

```sql
SELECT party_id, created_at, last_contacted_at, cadence_days, avatar_color, role, deleted_at
      FROM people_profile
      WHERE deleted_at IS NULL
      ORDER BY created_at DESC, party_id DESC
      LIMIT ?
```

```
SCAN people_profile
USE TEMP B-TREE FOR ORDER BY
```

### _shared/journal.scheme (people/journal)

```sql
SELECT scheme_id, uri
      FROM core_concept_scheme
      WHERE uri = ?
      ORDER BY scheme_id ASC, scheme_id ASC
      LIMIT ?
```

```
SEARCH core_concept_scheme USING INDEX sqlite_autoindex_core_concept_scheme_2 (uri=?)
```

### people.journal.concepts (people/journal)

```sql
SELECT concept_id, notation
      FROM core_concept

      ORDER BY concept_id ASC, concept_id ASC
      LIMIT ?
```

```
SCAN core_concept USING INDEX sqlite_autoindex_core_concept_1
```

### people.journal.activityLinks (people/journal)

```sql
SELECT link_id, from_type, from_id, to_type, to_id
      FROM core_link
      WHERE from_type = ? AND to_type = ? AND valid_to IS NULL
      ORDER BY link_id ASC, link_id ASC
      LIMIT ?
```

```
SEARCH core_link USING INDEX idx_link_to (to_type=?)
USE TEMP B-TREE FOR ORDER BY
```

### people.trash.profiles (people/trash)

```sql
SELECT party_id, role, deleted_at, purge_at
      FROM people_profile
      WHERE deleted_at IS NOT NULL
      ORDER BY deleted_at DESC, party_id DESC
      LIMIT ?
```

```
SCAN people_profile
USE TEMP B-TREE FOR ORDER BY
```

### people.history.revisions (people/history)

```sql
SELECT revision_id, entity_type, entity_id, operation, snapshot_json, recorded_at, undo_until, undone_at
      FROM core_entity_revision
      WHERE entity_type = ? AND entity_id = ?
      ORDER BY recorded_at DESC, revision_id DESC
      LIMIT ?
```

```
SEARCH core_entity_revision USING INDEX core_entity_revision_entity_idx (entity_type=? AND entity_id=?)
USE TEMP B-TREE FOR LAST TERM OF ORDER BY
```

## photos

### photos.storage.rollup (photos/storage)

```sql
SELECT bucket, item_count, byte_size, computed_at
      FROM blob_custody_rollup

      ORDER BY bucket ASC, bucket ASC
      LIMIT ?
```

```
SCAN blob_custody_rollup USING INDEX sqlite_autoindex_blob_custody_rollup_1
```

### photos.library.live (photos/library)

```sql
SELECT asset_id, content_id, kind, title, captured_at, tz_offset_min, capture_group_id, place_id, camera_device_id, width, height, duration_s, source_asset_id, archived_at, deleted_at, purge_at, created_at, updated_at
      FROM media_asset
      WHERE deleted_at IS NULL AND archived_at IS NULL AND captured_at < ?
      ORDER BY captured_at DESC, asset_id DESC
      LIMIT ?
```

```
SCAN media_asset
USE TEMP B-TREE FOR ORDER BY
```

### photos.library.trash (photos/library)

```sql
SELECT asset_id, content_id, kind, title, captured_at, tz_offset_min, capture_group_id, place_id, camera_device_id, width, height, duration_s, source_asset_id, archived_at, deleted_at, purge_at, created_at, updated_at
      FROM media_asset
      WHERE deleted_at IS NOT NULL
      ORDER BY deleted_at DESC, asset_id DESC
      LIMIT ?
```

```
SCAN media_asset
USE TEMP B-TREE FOR ORDER BY
```

### photos.library.albums (photos/library)

```sql
SELECT collection_id, name, cover_content_id
      FROM core_collection

      ORDER BY collection_id ASC, collection_id ASC
      LIMIT ?
```

```
SCAN core_collection USING INDEX sqlite_autoindex_core_collection_1
```

### photos.shared.places (photos/library)

```sql
SELECT place_id, name, geo_lat, geo_lng, kind, address_json
      FROM core_place

      ORDER BY place_id ASC, place_id ASC
      LIMIT ?
```

```
SCAN core_place USING INDEX sqlite_autoindex_core_place_1
```

### photos.shared.schemes (photos/library)

```sql
SELECT scheme_id, uri
      FROM core_concept_scheme

      ORDER BY scheme_id ASC, scheme_id ASC
      LIMIT ?
```

```
SCAN core_concept_scheme USING INDEX sqlite_autoindex_core_concept_scheme_1
```

### photos.shared.concepts (photos/library)

```sql
SELECT concept_id, scheme_id, pref_label, notation
      FROM core_concept

      ORDER BY concept_id ASC, concept_id ASC
      LIMIT ?
```

```
SCAN core_concept USING INDEX sqlite_autoindex_core_concept_1
```

### photos.faces.regions (photos/faces)

```sql
SELECT region_id, asset_id, bbox_json, party_id, confidence, confirmed_by_party_id, review_state
      FROM media_face_region
      WHERE asset_id = ?
      ORDER BY region_id ASC, region_id ASC
      LIMIT ?
```

```
SEARCH media_face_region USING INDEX idx_face_region_asset (asset_id=?)
USE TEMP B-TREE FOR ORDER BY
```

### photos.faces.parties (photos/faces)

```sql
SELECT party_id, display_name, kind
      FROM core_party

      ORDER BY display_name ASC, party_id ASC
      LIMIT ?
```

```
SCAN core_party
USE TEMP B-TREE FOR ORDER BY
```

### photos.faceQueue.regions (photos/face-queue)

```sql
SELECT region_id, asset_id, bbox_json, party_id, confidence, confirmed_by_party_id, review_state, created_at
      FROM media_face_region

      ORDER BY region_id ASC, region_id ASC
      LIMIT ?
```

```
SCAN media_face_region USING INDEX sqlite_autoindex_media_face_region_1
```

### photos.people.regions (photos/people)

```sql
SELECT region_id, asset_id, bbox_json, party_id, confidence, confirmed_by_party_id, review_state
      FROM media_face_region

      ORDER BY region_id ASC, region_id ASC
      LIMIT ?
```

```
SCAN media_face_region USING INDEX sqlite_autoindex_media_face_region_1
```

### photos.people.clusters (photos/people)

```sql
SELECT region_id, cluster_id, computed_at
      FROM media_face_cluster

      ORDER BY region_id ASC, region_id ASC
      LIMIT ?
```

```
SCAN media_face_cluster USING INDEX sqlite_autoindex_media_face_cluster_1
```

### photos.duplicates.phashes (photos/duplicates)

```sql
SELECT asset_id, phash, cluster_id, computed_at
      FROM media_asset_phash
      WHERE cluster_id IS NOT NULL
      ORDER BY cluster_id ASC, asset_id ASC
      LIMIT ?
```

```
SEARCH media_asset_phash USING INDEX idx_media_asset_phash_cluster (cluster_id>?)
USE TEMP B-TREE FOR LAST TERM OF ORDER BY
```

### photos.enrichment.policy (photos/enrichment-status)

```sql
SELECT domain, tier, updated_at
      FROM enrich_policy
      WHERE domain = ?
      ORDER BY domain ASC, domain ASC
      LIMIT ?
```

```
SEARCH enrich_policy USING INDEX sqlite_autoindex_enrich_policy_1 (domain=?)
```

## tally

### tally.dashboard.vault (tally/dashboard)

```sql
SELECT vault_id, self_party_id, base_currency
      FROM core_vault

      ORDER BY vault_id ASC, vault_id ASC
      LIMIT ?
```

```
SCAN core_vault USING INDEX sqlite_autoindex_core_vault_1
```

### tally.dashboard.friends (tally/dashboard)

```sql
SELECT friend_id, party_id, created_at
      FROM tally_friend

      ORDER BY friend_id ASC, friend_id ASC
      LIMIT ?
```

```
SCAN tally_friend USING INDEX sqlite_autoindex_tally_friend_1
```

### tally.dashboard.groups (tally/dashboard)

```sql
SELECT group_id, circle_id, icon, color, simplify_opt_in, archived_at, currency
      FROM tally_group

      ORDER BY group_id ASC, group_id ASC
      LIMIT ?
```

```
SCAN tally_group USING INDEX sqlite_autoindex_tally_group_1
```

### tally.dashboard.circles (tally/dashboard)

```sql
SELECT circle_id, owner_party_id, name, kind
      FROM social_circle

      ORDER BY circle_id ASC, circle_id ASC
      LIMIT ?
```

```
SCAN social_circle USING INDEX sqlite_autoindex_social_circle_1
```

### tally.dashboard.circleMembers (tally/dashboard)

```sql
SELECT member_id, circle_id, party_id
      FROM social_circle_member

      ORDER BY member_id ASC, member_id ASC
      LIMIT ?
```

```
SCAN social_circle_member USING INDEX sqlite_autoindex_social_circle_member_1
```

### tally.dashboard.expenses (tally/dashboard)

```sql
SELECT expense_id, group_id, description, amount_minor, currency, paid_by, split_method, split_params_json, spent_on, category, txn_id, created_at, updated_at
      FROM tally_expense
      WHERE deleted_at IS NULL
      ORDER BY spent_on DESC, expense_id DESC
      LIMIT ?
```

```
SCAN tally_expense
USE TEMP B-TREE FOR ORDER BY
```

### tally.dashboard.splits (tally/dashboard)

```sql
SELECT expense_id, party_id, share_minor
      FROM tally_expense_split

      ORDER BY expense_id ASC, party_id ASC
      LIMIT ?
```

```
SCAN tally_expense_split USING INDEX sqlite_autoindex_tally_expense_split_1
```

### tally.dashboard.payers (tally/dashboard)

```sql
SELECT expense_id, party_id, paid_minor
      FROM tally_expense_payer

      ORDER BY expense_id ASC, party_id ASC
      LIMIT ?
```

```
SCAN tally_expense_payer USING INDEX sqlite_autoindex_tally_expense_payer_1
```

### tally.dashboard.settlements (tally/dashboard)

```sql
SELECT settlement_id, group_id, from_party, to_party, amount_minor, currency, paid_on, txn_id, created_at
      FROM tally_settlement
      WHERE deleted_at IS NULL
      ORDER BY settlement_id ASC, settlement_id ASC
      LIMIT ?
```

```
SCAN tally_settlement USING INDEX sqlite_autoindex_tally_settlement_1
```

### tally.dashboard.obligations (tally/dashboard)

```sql
SELECT obligation_id, from_party, to_party, amount_minor, currency, reason, incurred_on, settled_at
      FROM tally_obligation
      WHERE settled_at IS NULL AND deleted_at IS NULL
      ORDER BY obligation_id ASC, obligation_id ASC
      LIMIT ?
```

```
SCAN tally_obligation USING INDEX sqlite_autoindex_tally_obligation_1
```

### tally.dashboard.receipts (tally/dashboard)

```sql
SELECT attachment_id, target_type, target_id, content_id, role, is_primary
      FROM core_attachment
      WHERE target_type = ? AND role = ?
      ORDER BY attachment_id ASC, attachment_id ASC
      LIMIT ?
```

```
SEARCH core_attachment USING INDEX idx_attachment_target (target_type=?)
USE TEMP B-TREE FOR ORDER BY
```

### tally.dashboard.receiptLines (tally/dashboard)

```sql
SELECT line_item_id, expense_id, receipt_id, kind, description, amount_minor, sort_order
      FROM tally_expense_line_item

      ORDER BY line_item_id ASC, line_item_id ASC
      LIMIT ?
```

```
SCAN tally_expense_line_item USING INDEX sqlite_autoindex_tally_expense_line_item_1
```

### tally.dashboard.receiptAllocations (tally/dashboard)

```sql
SELECT line_item_id, party_id, share_minor
      FROM tally_expense_line_allocation

      ORDER BY line_item_id ASC, party_id ASC
      LIMIT ?
```

```
SCAN tally_expense_line_allocation USING INDEX sqlite_autoindex_tally_expense_line_allocation_1
```

### tally.dashboard.nudges (tally/dashboard)

```sql
SELECT nudge_id, party_id, group_id, as_of_minor, note, prepared_at, created_at
      FROM tally_nudge

      ORDER BY prepared_at DESC, nudge_id DESC
      LIMIT ?
```

```
SCAN tally_nudge
USE TEMP B-TREE FOR ORDER BY
```

### tally.dashboard.parties (tally/dashboard)

```sql
SELECT party_id, display_name
      FROM core_party
      WHERE party_id IN (?)
      ORDER BY party_id ASC, party_id ASC
      LIMIT ?
```

```
SEARCH core_party USING INDEX sqlite_autoindex_core_party_1 (party_id=?)
```

### tally.dashboard.trash (tally/dashboard)

```sql
SELECT expense_id, group_id, description, amount_minor, currency, paid_by, spent_on, category, deleted_at, purge_at
      FROM tally_expense
      WHERE deleted_at IS NOT NULL
      ORDER BY deleted_at DESC, expense_id DESC
      LIMIT ?
```

```
SCAN tally_expense
USE TEMP B-TREE FOR ORDER BY
```

### tally.dashboard.recurring (tally/dashboard)

```sql
SELECT template_id, group_id, description, original_amount_minor, original_currency, settlement_currency, paid_by, category, rrule, anchor_start, tz, rate_scaled, rate_scale, rate_source, rate_date, status, last_materialized_start
      FROM tally_recurring_expense

      ORDER BY updated_at DESC, template_id DESC
      LIMIT ?
```

```
SCAN tally_recurring_expense
USE TEMP B-TREE FOR ORDER BY
```

### tally.dashboard.recurringExceptions (tally/dashboard)

```sql
SELECT exception_id, target_type, target_id, original_start_local, recurrence_semantics, scope, action, override_json
      FROM schedule_recurrence_exception
      WHERE target_type = ?
      ORDER BY exception_id ASC, exception_id ASC
      LIMIT ?
```

```
SEARCH schedule_recurrence_exception USING INDEX schedule_recurrence_exception_target_idx (target_type=?)
USE TEMP B-TREE FOR ORDER BY
```

### tally.history.revisions (tally/history)

```sql
SELECT revision_id, entity_type, entity_id, operation, snapshot_json, recorded_at, undo_until, undone_at
      FROM core_entity_revision
      WHERE entity_type = ? AND entity_id = ?
      ORDER BY recorded_at DESC, revision_id DESC
      LIMIT ?
```

```
SEARCH core_entity_revision USING INDEX core_entity_revision_entity_idx (entity_type=? AND entity_id=?)
USE TEMP B-TREE FOR LAST TERM OF ORDER BY
```

### tally.matches.transactions (tally/matches)

```sql
SELECT txn_id, account_id, posted_at, amount_minor, currency, direction, description
      FROM core_transaction
      WHERE status <> ?
      ORDER BY posted_at DESC, txn_id DESC
      LIMIT ?
```

```
SCAN core_transaction
USE TEMP B-TREE FOR ORDER BY
```

## tasks

### tasks.board.open (tasks/board)

```sql
SELECT task_id, parent_task_id, project_id, section_id, status, title, description, priority, due_at, completed_at, effort_min, rrule, tz, recurrence_anchor, series_id, sort_order, created_at, updated_at
      FROM schedule_task
      WHERE status IN (?, ?)
      ORDER BY created_at DESC, task_id DESC
      LIMIT ?
```

```
SCAN schedule_task
USE TEMP B-TREE FOR ORDER BY
```

### tasks.board.logbook (tasks/board)

```sql
SELECT task_id, parent_task_id, project_id, section_id, status, title, description, priority, due_at, completed_at, effort_min, rrule, tz, recurrence_anchor, series_id, sort_order, created_at, updated_at
      FROM schedule_task
      WHERE status IN (?, ?)
      ORDER BY completed_at DESC, task_id DESC
      LIMIT ?
```

```
SCAN schedule_task
USE TEMP B-TREE FOR ORDER BY
```

### tasks.board.projects (tasks/board)

```sql
SELECT project_id, name, area, color, sort_order
      FROM schedule_project
      WHERE archived_at IS NULL
      ORDER BY sort_order ASC, project_id ASC
      LIMIT ?
```

```
SCAN schedule_project
USE TEMP B-TREE FOR ORDER BY
```

### tasks.board.sections (tasks/board)

```sql
SELECT section_id, project_id, name, sort_order
      FROM schedule_section

      ORDER BY sort_order ASC, section_id ASC
      LIMIT ?
```

```
SCAN schedule_section
USE TEMP B-TREE FOR ORDER BY
```
