/** Single source of truth lives in `@centraid/core/protocol` — do not re-declare version constants here. */

export {
  GATEWAY_VERSION,
  GATEWAY_PROTOCOL_VERSION,
  GATEWAY_MIN_PROTOCOL_VERSION,
} from "@centraid/core/protocol";
