{
  "name": "@centraid/agent-runtime",
  "version": "0.1.0",
  "private": false,
  "description": "Unified local harness runtime for Centraid: drives one turn over the Agent Client Protocol for every harness kind, launching first-party ACP adapters for CLIs that lack an ACP mode. Used by both the per-app data conversation and the workspace-capable assistant thread. Ships a tiny `centraid` CLI bin harnesses can invoke.",
  "license": "SEE LICENSE IN REPOSITORY",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/srikanth235/centraid.git",
    "directory": "packages/agent-runtime"
  },
  "bin": {
    "centraid": "./dist/cli/centraid-cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/backends/acp && cp src/backends/acp/vault-mcp-stdio-proxy.mjs dist/backends/acp/",
    "clean": "rm -rf dist",
    "probe:all-harnesses": "bun scripts/probe-all-harnesses.ts",
    "test": "vitest run",
    "test:live-automation-failover": "CENTRAID_LIVE_AUTOMATION=1 vitest run src/automation/live-automation-failover.test.ts",
    "test:live-harnesses": "bun scripts/live-harness-smoke.ts",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "0.63.0",
    "@agentclientprotocol/codex-acp": "1.1.7",
    "@agentclientprotocol/sdk": "1.3.0",
    "@centraid/app-engine": "workspace:*",
    "@centraid/automation": "workspace:*"
  },
  "devDependencies": {
    "@centraid/test-kit": "workspace:*",
    "@types/node": "^26.1.2",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5"
  }
}
