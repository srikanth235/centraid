{
  "name": "@centraid/apps-store",
  "version": "0.1.0",
  "private": true,
  "description": "Gateway-owned git store for centraid app code. Owns a bare repo + per-session worktrees for editing + one materialized-main worktree the runtime reads from. Publish = path-scoped commit + tag + ff-merge into main + atomic worktree swap. Rollback is forward-only.",
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@centraid/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.3",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5"
  }
}
