{
  "name": "@centraid/stores",
  "version": "0.1.0",
  "private": true,
  "description": "Gateway-scoped stores that aren't the per-app run ledger: central analytics (push-based run summaries) and the read-only Insights aggregation over them. Depends on @centraid/app-engine for the DatabaseProvider seam and the RunSummary/RunSummarySink contract; app-engine never depends back.",
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@centraid/app-engine": "workspace:*"
  },
  "devDependencies": {
    "@centraid/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.3",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5"
  }
}
