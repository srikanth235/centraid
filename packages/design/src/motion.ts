// Centraid — motion primitives.
// One easing curve for the whole product: a calm, instrument-grade ease-out
// that both emitters publish as `--ease`. Shell and blueprint surfaces must
// not spell this role twice, so the literal lives here and nowhere else.

export const EASE = "cubic-bezier(0.2, 0.7, 0.3, 1)";
