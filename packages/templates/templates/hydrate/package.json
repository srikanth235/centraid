{
  "name": "centraid-app-hydrate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@centraid/openclaw-plugin": "*",
    "typescript": "^5.6.0"
  }
}
