{
  "name": "@centraid/vault",
  "version": "0.1.0",
  "private": false,
  "description": "Duaility personal ontology vault — the canonical, person-owned data model (core spine + consent plane + agent plane + 8 life domains) over local-first SQLite, and the gateway: the sole holder of connections, where every read and typed command passes one pipeline (identity → consent → contract → execution → evidence) and leaves receipts. Implements the logical model in duaility-ontology.html.",
  "license": "SEE LICENSE IN REPOSITORY",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/srikanth235/centraid.git",
    "directory": "packages/vault"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@centraid/backup": "workspace:*",
    "@centraid/core": "workspace:*",
    "hash-wasm": "^4.12.0",
    "uuid": "^14.0.2"
  },
  "devDependencies": {
    "@centraid/test-kit": "workspace:*",
    "@types/node": "^26.1.2",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5"
  }
}
