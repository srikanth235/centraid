{
  "name": "@centraid/chat-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Pi-coding-agent customization for the centraid in-app DATA CHAT: agent that answers questions and runs SQL against a single deployed app's SQLite over the runtime HTTP surface. Works against both the embedded local runtime and remote OpenClaw. (For app authoring see @centraid/builder-harness.)",
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@centraid/builder-harness": "workspace:*",
    "@centraid/runtime-core": "workspace:*",
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "typebox": "^1.1.24"
  },
  "devDependencies": {
    "@centraid/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.3",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5"
  }
}
