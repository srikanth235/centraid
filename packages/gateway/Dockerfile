# Deprecated build path (issue #501 scaffold).
# Canonical image: monorepo-root Dockerfile (issue #504 packaging) — native iroh
# tunnel, non-root, HEALTHCHECK, Host allowlist, durable /data.
#
#   docker build -f Dockerfile -t centraid-gateway:local .
#
# Release workflow release-gateway-image.yml builds that root file.
# This file is intentionally not a second implementation.
