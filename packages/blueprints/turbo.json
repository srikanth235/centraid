{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**", "apps/*/actions/*.js", "apps/*/queries/*.js"]
    },
    "typecheck": {
      "dependsOn": ["^build", "@centraid/server#build"]
    }
  }
}
