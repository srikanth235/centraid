# @centraid/blueprints

Bundled, pre-built Centraid apps that the desktop gallery offers as **clone and deploy** starting points. Templates are split by kind: full UI apps live under `apps/<id>/` and automation apps under `automations/<id>/`. Each is a fully-formed app — identical in shape to an app a user authors themselves (`index.html`, `app.css`, `app.js`, `queries/`, `actions/`, `migrations/`, `app.json`).

## Layout

```
packages/blueprints/
  src/
    index.ts        — listTemplates(), resolveTemplates(), fetchRemoteTemplates(), appTemplatesDir
    types.ts        — TemplateMeta, TemplateManifest, AppKnob
    scaffold*.ts    — blank-app scaffolders (scaffoldApp / scaffoldAppFiles)
    clone.ts        — cloneTemplate / cloneTemplateFiles + clone-identity helpers
  index.json        — manifest source (hand-edited TemplateManifest)
  manifest.json     — generated by scripts/build-manifest.mjs (checked in)
  apps/<id>/        — one folder per UI-app template (kind: 'app')
  automations/<id>/ — one folder per automation template (kind: 'automation')
```

The bundled tree doubles as the remote tree, so a GitHub-raw URL serving `manifest.json` + `<apps|automations>/<id>/<file>` works as a remote template source unchanged.

The package carries both halves of "how a new app comes into being": the **blank scaffolders** (`scaffoldApp`/`scaffoldAppFiles`, moved here from `@centraid/app-engine` in #151) and the **template clone** path. Its runtime code depends only on `@centraid/design-tokens` — no engine, no store. Consumed by `@centraid/gateway` (lifecycle routes) and `@centraid/automation` (the `ScaffoldFile` contract).

## Shared browser runtimes

Apps resolve a small canonical asset set from `kit/` instead of copying it into every clone. React remains the checked-in runtime bundle described by `scripts/vendor-react.mjs`. `scripts/vendor-browser-shared.mjs` bundles the canonical `@centraid/blob-format` and client video-frame sources to `kit/blob-format.js` and `kit/video-frame.js` for browser consumers. PDF text extraction uses the lockfile-pinned `pdfjs-dist` development dependency: `scripts/vendor-pdfjs.mjs` copies its minified display and worker modules to `kit/pdf.min.mjs` and `kit/pdf.worker.min.mjs`. Those generated files are gitignored and are rebuilt by `build`, `build:manifest`, and `test`; they are served same-origin by the app engine, so both protocols remain offline and compatible with the app CSP. Do not hand-edit them.

## Adding a template

1. Drop the app folder at `apps/<id>/` (or `automations/<id>/` for an automation) with the standard layout (`index.html`, `app.css`, `app.js`, `queries/`, `actions/`, `migrations/`, `package.json`, `app.json`). A UI-app template ships `index.html` + assets; an automation template (`app.json#kind = "automation"`) ships no UI — just `app.json` + `automations/<id>/{automation.json,handler.js}`.
2. **Ship compiled `.js` handlers**: templates carry only the runtime `.js` files (no `.ts` source, no `tsconfig.json`) so cloning is "click and deploy" — the gateway loads `.js` directly, with no `bun install` or compile step on first use. Author in TypeScript out-of-tree if you like, but commit the emitted `.js`.
3. Add an entry to `index.json` with `id`, `name`, `desc`, `colorKey`, `iconKey`, `version`. Automation templates add `kind: 'automation'` plus the gallery display fields (`emoji`, `category`, `triggerKind`, `triggerLabel`, `integrations`); UI apps omit all of these.
4. Run `bun run build:manifest` (also part of `bun run build`) to regenerate `manifest.json` from `index.json` — the build script walks each template folder, enumerates its `files[]`, and folds in `app.json#knobs` as `appKnobs`. `manifest.json` is checked into git so a GitHub-raw URL can serve it as the remote manifest unchanged.

The `version` field follows semver. Bump it when the template's source changes.

The kind-segment directory (`apps/` vs `automations/`) is **derived from `kind`**, not stored separately — the manifest build, the on-disk resolver (`templateSourceDir`), and the remote fetcher (`downloadTemplate`) all compute it the same way via `templateKindDir`.

## Cloning at runtime

The gateway owns the template catalog. It resolves bundle-or-cache (preferring the higher semver) and:

- serves display metadata at `GET /centraid/_templates` (`makeTemplatesRouteHandler` → `resolveTemplates()`),
- clones a chosen template with `POST /centraid/_apps/_clone` (`{ templateId, publish? }`): the gateway reads the template's files (`readTemplateFiles`), rewrites them for a fresh, non-colliding `(id, name)` (`suggestCloneIdentityFrom` + `cloneTemplateFiles`), mints any pending webhook secrets, and stages the result into a draft git-store session. Passing `publish: true` flips the draft live in one call.

`cloneTemplateFiles` rewrites `app.json` (fresh `id`/`name`, `version` → `"0.1.0"`, carried `description`), `package.json#name` → `centraid-app-<id>` (only when it followed the convention), the `index.html` `<title>`, and each `automations/<id>/automation.json#name` (re-stamping `generated`); it seeds an `automations/README.md` brief when the template ships no automations.

## Remote template resolver

`fetchRemoteTemplates({ cacheDir, remoteUrl })` fetches `<remoteUrl>/manifest.json` and downloads any template whose remote `version` strictly beats the local best (max of bundle + cache). Files are written atomically into `<cacheDir>/<apps|automations>/<id>/...`; the cache manifest is written last so a torn fetch never points users at incomplete code. It **never throws** — offline, a 404, or a bad manifest leaves the cache untouched and callers keep resolving to the bundle. `resolveTemplates({ cacheDir })` then merges bundle + cache per template, surfacing cache-only templates (added remotely, not yet bundled) too.
