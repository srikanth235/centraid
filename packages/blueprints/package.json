{
  "name": "@centraid/blueprints",
  "version": "0.1.0",
  "private": true,
  "description": "How a new centraid app comes into being: blank-app scaffolders, template clone, and the bundled template gallery (pre-built apps + remote-fetch resolver) the desktop offers as clone-and-deploy starting points.",
  "files": [
    "dist",
    "apps",
    "automations",
    "kit",
    "index.json",
    "manifest.json",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "vendor:pdfjs": "node scripts/vendor-pdfjs.mjs",
    "build:manifest": "bun run vendor:pdfjs && node scripts/build-manifest.mjs",
    "build": "bun run build:manifest && tsc -p tsconfig.build.json",
    "lint": "node scripts/lint-apps.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun run vendor:pdfjs && vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@centraid/design-tokens": "workspace:*"
  },
  "devDependencies": {
    "@centraid/app-engine": "workspace:*",
    "@centraid/client": "workspace:*",
    "@centraid/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "pdfjs-dist": "5.7.284",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5"
  }
}
