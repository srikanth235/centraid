{
  "id": "centraid",
  "name": "Centraid",
  "description": "Mounts /centraid on the gateway. Each registered app is a folder with static assets, a sqlite database, and JS handlers for queries / actions.",
  "contracts": {
    "tools": ["centraid_describe", "centraid_read", "centraid_write"]
  },
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "appsDir": {
        "type": "string",
        "description": "Where app folders live. Absolute paths are used as-is; relative paths resolve under OpenClaw's state dir ($OPENCLAW_STATE_DIR, default ~/.openclaw). Default: '~/.openclaw/centraid'.",
        "default": "centraid"
      }
    }
  }
}
