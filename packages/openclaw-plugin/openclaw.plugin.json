{
  "id": "centraid",
  "name": "Centraid",
  "description": "Mounts /centraid on the gateway. Each registered app is a folder with static assets, a sqlite database, and JS handlers for queries / actions / cron-fed ingest.",
  "contracts": {
    "tools": ["centraid_sql_describe", "centraid_sql_read", "centraid_sql_write"]
  },
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "appsDir": {
        "type": "string",
        "description": "Where app folders live. Absolute paths are used as-is; relative paths resolve under OpenClaw's state dir ($OPENCLAW_STATE_DIR, default ~/.openclaw). Default: '~/.openclaw/centraid'.",
        "default": "centraid"
      },
      "gatewayBaseUrl": {
        "type": "string",
        "description": "Loopback URL used to point cron webhooks back at the gateway. Must resolve to 127.0.0.1.",
        "default": "http://127.0.0.1:18789"
      },
      "versionRetention": {
        "type": "integer",
        "description": "Maximum number of versions to retain per uploaded app (active version always kept). Minimum 2.",
        "default": 5,
        "minimum": 2
      }
    }
  }
}
