{
  "name": "@centraid/local-chat-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Local-runtime ChatRunner for centraid's per-app chat. Spawns a user-configured coding CLI (Codex or Claude Code) as a subprocess and attaches a stdio MCP server that exposes centraid_sql_* tools scoped to the active app. Centraid bundles no inference loop — the user brings their own CLI.",
  "bin": {
    "centraid-mcp-server": "./dist/centraid-mcp-server.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@centraid/runtime-core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@centraid/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.3",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5"
  }
}
