{
  "name": "@centraid/local-chat-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Local-runtime ChatRunner for centraid's per-app chat. Spawns a user-configured coding CLI (Codex or Claude Code) as a subprocess with cwd pinned to the active app's data dir and teaches it about a small `centraid` CLI bin that reads/writes data.sqlite directly. No MCP, no network, no token plumbing.",
  "bin": {
    "centraid": "./dist/centraid-cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@centraid/runtime-core": "workspace:*"
  },
  "devDependencies": {
    "@centraid/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.3",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5"
  }
}
